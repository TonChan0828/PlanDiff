# 開発計画 — PlanDiff

**最終更新**: 2026-08-13(P10-1「提案経由の定期予定の自動学習補正」完了)
**運用ルール**:
- 実装着手時はこの表の「次に着手」から選び、`.claude/skills/spec-driven-dev` のワークフロー(仕様書作成 → ユーザー承認 → シナリオ展開 → TDD)で進める
- 着手時に「状態」を更新し、仕様書を作成したら「仕様書」列にパスを記入する
- 優先度・スコープの変更はユーザーの承認を得てからこのファイルに反映する

**優先度の定義**:
- **P0**: 外部リードタイムがある/全機能の前提。最優先で着手
- **P1**: MVPのコア体験。P0完了後に順次
- **P2**: MVPに含むが、コア体験の後でよいもの
- **P3**: MVP公開準備(ベータ投入に必要)
- **v2**: MVPスコープ外。**先回りで実装しない**(CLAUDE.md参照)

**状態**: `未着手` / `仕様書作成中` / `承認待ち` / `実装中` / `完了`

---

## Phase 0 — 基盤(P0)

外部リードタイム(OAuth審査は数週間かかりうる)と、全機能の前提になるものを最優先で片付ける。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P0-1 | Supabaseプロジェクト作成・`.env.local`設定・`supabase link` | P0 | — | 完了 | — | 完了(2026-07-05)。ref: bkcppesfpayjtftlfmoz(東京)。`db push` でリモートスキーマ適用済み。ローカル開発・テストはローカルSupabaseを継続使用 |
| P0-2 | Google Cloud OAuthクライアント作成・同意画面設定 | P0 | — | 完了 | — | 完了(2026-07-05)。同意画面は**テスト中**(審査申請はP0-5)。スコープは `calendar.events.readonly` のみ。リダイレクトURIはローカル/リモート両方登録済み |
| P0-3 | プライバシーポリシー・利用規約ページ(最小版) | P0 | — | 完了 | [specs/P0-3](specs/P0-3_プライバシーポリシーと利用規約.md) | 完了(2026-07-05。条文レビュー済み、運営者名=Sho Takasaki)。**`[制定日]` はP0-4デプロイ時に確定値へ差し替える**。公開はP0-4で |
| P0-4 | Vercelデプロイ(本番URL確保) | P0 | 初回コミット | 完了 | — | 完了(2026-07-06)。本番URL: **https://plandiff.vercel.app**(tonchans-projects/plandiff、GitHub連携済み)。制定日を2026-07-06で確定。**残: SupabaseのURL Configuration更新(ユーザー作業)** |
| P0-5 | OAuth審査申請 | P0 | P0-2, P0-3, P0-4 | 保留(凍結) | — | Google連携UIをP2-5で凍結したため保留(2026-07-10)。凍結解除の判断時に再開する |
| P0-6 | DBスキーマ+RLSマイグレーション(4テーブル) | P0 | P0-1 | 完了 | [specs/P0-6](specs/P0-6_DBスキーマとRLS.md) | 完了(2026-07-05。ローカル検証まで=仕様書スコープ)。結合テスト10件合格。**リモートへの適用(db push)はP0-1完了後** |

## Phase 1 — 認証とカレンダー連携(P1 / Week 1相当)

最大の技術リスク(refresh token取得)をここで潰す。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 対応FR |
|---|---|---|---|---|---|---|
| P1-1 | ~~Google認証(ログイン/ログアウト、`access_type=offline` + `prompt=consent`、refresh token保存、**取得失敗時の再認可導線**)~~ → **P1-3に置き換え** | P1 | P0-1, P0-2, P0-6 | 完了(置換) | [specs/P1-1](specs/P1-1_Google認証.md) | FR-01(旧)。2026-07-05完了だが2026-07-09にP1-3へ引き継ぎ |
| P1-2 | カレンダー読み取り同期(トークン自動更新、`synced_events`キャッシュ、表示週±1週、手動リフレッシュ) | P1 | P1-1 | 実装中 | [specs/P1-2](specs/P1-2_カレンダー同期.md) | FR-02。実装+自動テスト55件+M3完了(2026-07-06)。M1はP2-1の手動疎通で確認済み(2026-07-07)。**残: M2(Google側の予定変更→「更新」で反映)とVercel環境変数追加** |
| P1-3 | メール認証(サインアップ/ログイン/メール確認/パスワード再設定)とGoogleカレンダー任意連携(認証と切り離した独自OAuth2フロー、`/calendar`のGoogle未連携UX) | P0 | P0-1, P0-6 | 完了 | [specs/P1-3](specs/P1-3_メール認証とGoogle任意連携.md) | FR-01・FR-02(改定)。2026-07-09に追加・仕様書承認・実装完了。実メールでのsignup確認・パスワードリセット一連を実機確認済み(`additional_redirect_urls`未登録・PKCE`code`交換方式の誤りを実地で発見し修正)。自動テスト(単体約65+結合約10)+`npm run check`合格+375px確認。**残: 本番Supabaseの認証設定(Redirect URLに`/auth/confirm`・`/auth/confirm-recovery`追加、メール確認必須化)、Google Cloud ConsoleへのコールバックURL登録(ユーザー作業)後のGoogle連携実機疎通(M3)** |

## Phase 2 — コア体験:カレンダーとタイマー(P1 / Week 2相当)

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 対応FR |
|---|---|---|---|---|---|---|
| P2-1 | カレンダービュー(日/週、予定ブロック時系列表示、モバイルファースト) | P1 | P1-2 | 完了 | [specs/P2-1](specs/P2-1_カレンダービュー.md) | FR-03。完了(2026-07-07)。自動テスト23件+手動疎通M1/M2合格(PR #3) |
| P2-2 | 予定連動タイマー(予定タップで開始/停止、実行中1本制約、新規開始で既存自動停止、リロード/複数デバイス復元) | P1 | P2-1, P0-6 | 完了 | [specs/P2-2](specs/P2-2_予定連動タイマー.md) | FR-04。完了(2026-07-07)。自動テスト18件+手動疎通M1/M2合格(PR #4) |
| P2-3 | フリータイマー(タイトル入力で開始/停止) | P1 | P2-2 | 完了 | [specs/P2-3](specs/P2-3_フリータイマー.md) | FR-05。完了(2026-07-07)。自動テスト11件+結合テスト9件+手動疎通M1/M2合格 |
| P2-4 | 実績の手動編集(開始/終了時刻・タイトル修正、削除) | P2 | P2-2 | 完了 | [specs/P2-4](specs/P2-4_実績の手動編集.md) | FR-08。完了(2026-07-08)。自動テスト21件(単体15+結合6)+手動疎通M1/M2合格 |
| P2-5 | アプリ内予定の作成・編集・削除(`synced_events` に source列追加、`app:<uuid>` キー)+Google連携のUI凍結(環境フラグ `GOOGLE_INTEGRATION_ENABLED`、既定OFF) | P1 | P2-1, P2-4, P0-6 | 完了 | [specs/P2-5](specs/P2-5_アプリ内予定とGoogle連携凍結.md) | FR-10(新設)・FR-02(凍結)。完了(2026-07-10)。自動テスト: 単体+14件(計186)・結合+9件(計56)全合格、`npm run check`合格、375pxでM1〜M3手動疎通済み。同期stale削除に`source='google'`フィルタでアプリ予定を保護。本番Supabase(bkcppesfpayjtftlfmoz)へ`db push`済み(source列。2026-07-10、ユーザー実行)。**残: 凍結解除時はVercelに`GOOGLE_INTEGRATION_ENABLED=true`を設定** |
| P2-6 | 計測画面(`/track`。タイマー開始/停止・直近予定からの開始・当日実績の確認・フリー実績の予定昇格導線) | P2 | P2-3, P2-5 | 完了 | [specs/P2-6](specs/P2-6_計測画面.md) | FR-11(新設)。完了(2026-07-11)。ユーザー要望から仕様承認(要件定義書v0.4)。DBスキーマ変更なし。自動テスト: 単体+15件(計224)・結合+3件(計64)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)でM1/M2疎通済み。**拡張(2026-07-20)**: 実績リストを当日のみ→取得範囲(週±1週間)の日付グループ表示に変更(ユーザー要望)。`filterTodayEntries`→`groupEntriesByDay`へ移行。シナリオS13〜S17追加、`npm run check`(412テスト)合格 |

## Phase 3 — 差別化機能(P1〜P2 / Week 3相当)

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 対応FR |
|---|---|---|---|---|---|---|
| P3-1 | オーバーレイ表示(予定=左寄せ薄い塗り、実績=右寄せ濃い塗り、ズレの視覚化)。**プロダクトの「顔」。UI品質に最も投資** | P1 | P2-2 | 完了 | [specs/P3-1](specs/P3-1_オーバーレイ表示.md) | FR-06。完了(2026-07-08)。自動テスト(単体7+結合6+DB結合1)+Playwright 375px確認(日/週/ダーク)合格。PR #8マージ済み |
| P3-2 | ギャップサマリー(今日/今週の計画vs実績とズレ%、予定単位一覧、未着手予定・割り込み実績の区別) | P1 | P2-2 | 完了 | [specs/P3-2](specs/P3-2_ギャップサマリー.md) | FR-07。完了(2026-07-08)。自動テスト(単体9+結合6)+Playwright 375px確認(今日/週/ダーク/カレンダー導線)+ユーザー実環境での最終確認合格 |
| P3-3 | PWA対応(manifest、ホーム画面追加、モバイルで主要操作完結) | P2 | P2-2 | 完了 | [specs/P3-3](specs/P3-3_PWA対応.md) | FR-09。完了(2026-08-10)。実装・自動テスト+build合格、PR #10マージ済み(2026-07-09)。M1(インストール可能性)はChrome DevTools Protocol経由で自動確認合格。**2026-08-10: M2の実機確認(iPhone)で「起動するたびにログインが求められる」不具合を発見・修正(PR #59)**。原因は `start_url: "/"` がLPを指しており、LPが認証状態を見ないため常にログインCTAが出ていたこと(セッション自体は生きていた)。`start_url: "/calendar"` へ変更し `id: "/"` / `scope: "/"` を明示。副次的に、`proxy.ts` の matcher が `/` を除外している(P6-1)ためLPしか開かないとセッションリフレッシュが走らず、iOS ITPの7日上限で実際に切れうる穴も塞いだ。仕様書§1・S1を改訂しS8/S9追加、自動テスト672→674件。**M2-a(アイコン再追加後の起動でログインを求められない)をユーザーが実機確認・合格**。M2-b/c(1時間後・数日後の再起動)は時間経過が必要なため経過観察とし、再発時に再起票する |

## Phase 4 — 公開準備(P3 / Week 4相当)　

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P4-1 | オンボーディング(3ステップ説明。Google連携凍結中のため連携許可ステップはスコープ外) | P3 | P1-1 | 完了 | [specs/P4-1](specs/P4-1_オンボーディング.md) | 完了(2026-07-11)。初回`/calendar`訪問時に表示、`profiles.onboarded_at`で既読管理。自動テスト: 単体+13件(計209)・結合+3件(計61)全合格、`npm run check`合格、Playwright 375px確認(ライト/ダーク、全ステップ・スキップ・はじめる→calendar・再閲覧導線)済み。設定画面に「使い方をもう一度見る」リンク追加。PR #14マージ済み・main反映済み。本番Supabase(bkcppesfpayjtftlfmoz)へ`onboarded_at`カラム追加のマイグレーション適用済み(2026-07-11、ユーザー実行)。残タスクなし |
| P4-2 | 設定画面(アカウント、連携状態、再認可、**データ全削除**、ログアウト) | P3 | P1-1 | 完了 | [specs/P4-2](specs/P4-2_設定画面.md) | データ全削除はOAuth審査要件(Limited Use)。完了(2026-07-11)。自動テスト: 単体+10件(計196)・結合+2件(計58)全合格、`npm run check`合格、Playwright 375pxで削除フロー疎通済み。連携状態・再認可は凍結中のため既存実装を維持 |
| P4-3 | LP(コンセプト訴求、スクリーンショット) | P3 | P3-1 | 完了 | [specs/D-3](specs/D-3_サマリーヒーローとLP.md) | 完了(2026-07-13)。**D-3として実装**・PR #21マージ済み。スクリーンショットの代わりにトークン準拠のCSSオーバーレイ図を採用 |
| P4-4 | 料金ページ(Pro近日公開、興味クリック計測) | P3 | P4-3 | 完了 | [specs/P4-4](specs/P4-4_料金ページ.md) | 完了(2026-07-13)。PR #23マージ済み。計測=`pro_interest_events`(view/click、個人情報なし)・価格非表示。単体+7(計277)・結合+3(計71)全合格、Playwright 375px/1280px(ライト/ダーク)M1確認済み。本番Supabaseへの`db push`は2026-08-07に適用済み。残タスクなし |
| P4-5 | 磨き込み(375px幅の表示確認、体感速度、エラーメッセージ日本語化の総点検) | P3 | Phase 3完了 | 完了 | [specs/P4-5](specs/P4-5_磨き込み.md) | 完了(2026-07-13)。PR #24マージ済み。A.エラー境界・404の日本語化/B.遷移中スケルトン/C.日本語化総点検(漏れ0確認)/D.375px総点検/E.トークン化漏れ11ファイル修正+successトークン新設(総点検で発見、同日追加承認)。自動テスト(単体284/結合71)・M1〜M3確認済み。残タスクなし |
| P4-6 | ベータユーザー投入(テストユーザー登録、募集チャネル実行) | P3 | P4-1〜P4-5 | 未着手 | — | 募集チャネルは未決(要件定義書 §14) |

## デザイン刷新 — 「時間のdiff」(2026-07-12 ユーザー承認で追加)

現状機能ベースのデザイン再検討から発生。モックで方向性承認済み(D-1+D-2のスコープで着手)。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| D-1 | デザイン基盤とアプリシェル(トークン@theme化、Arialフォントバグ修正、群青×柿パレット、下部タブバー、ピルナビ廃止、タイムライン全高化)+テーマ手動切替(D-1e) | P2 | Phase 2完了 | 完了 | [specs/D-1-2](specs/D-1-2_デザイン刷新.md) | 完了(2026-07-12)。自動テスト・`npm run check`・Playwright 375px(ライト/ダーク)合格。PR #19マージ済み(M2=マージ判断) |
| D-2 | オーバーレイ磨き込み(群青×柿の塗り分け、ズレの符号付きdiff表記、方眼背景、現在時刻チップ、実行中バー刷新) | P2 | D-1 | 完了 | [specs/D-1-2](specs/D-1-2_デザイン刷新.md) | 完了(2026-07-12)。D-1と同時実装・PR #19マージ済み |
| D-3 | サマリーのdiffヒーロー化+新アイデンティティでのLP(P4-3を兼ねる) | P3 | D-1, D-2 | 完了 | [specs/D-3](specs/D-3_サマリーヒーローとLP.md) | 完了(2026-07-13)。自動テスト(単体+11、計270)・`npm run check`・Playwright(375px/1280px、ライト/ダーク)合格。PR #21マージ済み ※マージ時のコンフリクト解消で本行が一時的に旧内容へ戻っていたのを修正 |
| D-4 | 計測ヒーロー(経過時間の特大表示)+計測中の開始時刻変更(`updateRunningStart`) | P2 | D-1, P2-6 | 完了 | [specs/D-4](specs/D-4_計測ヒーローと開始時刻変更.md) | 完了(2026-07-12)。自動テスト(単体+12/結合+4)・M1合格・PR #19マージ済み |
| D-5 | ロゴ作成(「枠とはみ出し」マーク=予定アウトライン+実績塗り。PWAアイコン刷新、SVG favicon化、ヘッダー3箇所にロゴマーク表示) | P3 | D-1 | 完了 | [specs/D-5](specs/D-5_ロゴ作成.md) | 完了(2026-07-19)。PR #37マージ済み。2026-07-19 ユーザー要望から発生。候補3案(重なる時間/ガントのズレ/枠とはみ出し)からC案を選定。`components/logo-mark.tsx`新規、`generateAppIcon`刷新、`app/icon.svg`追加+`favicon.ico`削除、`Wordmark`に`withMark`追加。自動テスト+9件(計371)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)確認済み |

## Phase 5 — 機能拡張(2026-07-14 ユーザー要望で追加)

ユーザー要望2件から発生。brainstormingで要件確定済み。P5-1 → P5-2 の順に着手する。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P5-1 | 定期予定(繰り返し予定。毎日/毎週/平日、この回のみ+全体の編集削除、無期限+任意終了日。ルール+オンデマンド実体化方式) | P2 | P2-5 | 完了 | [specs/P5-1](specs/P5-1_定期予定.md) | 完了(2026-07-14)。PR #26マージ済み。要件定義書FR-12追加。`recurring_rules`+`recurring_exceptions` の2テーブル追加。`rec:<ruleId>:<日付>` キーで synced_events へ冪等実体化し、タイマー/オーバーレイ/サマリーは無改修。自動テスト単体+29件・結合+9件(計314/79)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)でM1〜M3手動疎通済み。残タスクなし |
| P5-2 | 過去の計測実績からの予定提案(カレンダーに提案セクション、同タイトル×曜日×時間帯の繰り返し検出、この週だけ/毎週で受け入れ) | P2 | P5-1 | 完了 | [specs/P5-2](specs/P5-2_実績からの予定提案.md) | 完了(2026-07-15)。PR #28マージ済み。要件定義書FR-13追加(v0.6)。DB変更なし(純粋関数`computeSuggestions`+既存Action再利用)。自動テスト単体+31件・結合+4件(計345/83)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)でM1〜M3手動疎通済み。CIのUTC実行で発覚したTZDateコピーコンストラクタのTZ非引き継ぎを修正済み。残タスクなし |
| P5-3 | サマリー件数ステータス行(ヒーロー直下に予定件数・着手・未着手・割り込み件数と合計時間を1行表示) | P2 | P3-2, D-3 | 完了 | [specs/P5-3](specs/P5-3_サマリー件数ステータス行.md) | 完了(2026-07-18)。PR #33マージ済み。2026-07-18 ユーザー要望(サマリーの参照項目追加の検討)から発生し、方向性=「今の期間の振り返りを深める」で確定。DB変更なし・算出済みデータの表示追加のみ。自動テスト単体+7件(計353)・結合+1件(計84)全合格、`npm run check`合格、Playwright 375px(ダーク/ライト・今日/週・空データ)確認済み。見送った開始遅延・予定単位ズレ%は仕様書のスコープ外欄に将来検討として記録。残タスクなし |
| P5-4 | 実績からの再計測(実績クリックで同名・同紐づけの計測をワンアクション開始。導線=計測画面の今日の実績/カレンダー実績ブロック/コンテキストパネル) | P2 | P2-3, P2-4 | 完了 | [specs/P5-4](specs/P5-4_実績からの再計測.md) | 2026-07-19 ユーザー要望から発生。仕様承認済み(2026-07-19)。DB変更なし(既存`startTimerAction`再利用)。googleEventId引き継ぎ・ブロック上▶ボタンは描画高さ28px(30分)以上のみ表示。自動テスト単体+9件(計362)・結合+1件(計85)全合格、`npm run check`合格、Playwright 375px/1280px(ライト/ダーク)で3導線+自動停止を手動疎通済み。PR #35マージ済み(2026-07-19)。残タスクなし |
| P5-5 | 時刻ステッパー入力(実績編集・開始時刻変更・アプリ内予定編集の`datetime-local`を、分00→59で時も−1する桁跨ぎ対応の独自ステッパーに置換) | P2 | P2-4, D-4, P2-5 | 完了 | [specs/P5-5](specs/P5-5_時刻ステッパー入力.md) | 完了(2026-07-20)。2026-07-20 ユーザー要望から発生(ネイティブdatetime-localは分の折り返しが時に波及しないため)。DB変更なし。自動テスト単体+35件・結合+3件(計409)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)で3パネルの桁跨ぎ動作を手動疎通済み。PR #39(マージ待ち) |
| P5-6 | 日時セグメント編集(P5-5の3ブロックを年/月/日 時:分の統合セグメント入力へ作り替え。← →でセグメント移動・↑↓で増減・数字で自動送り、共有▲▼+カレンダーボタン) | P2 | P5-5 | 実装完了・PR作成待ち | [specs/P5-6](specs/P5-6_日時セグメント編集.md) | 2026-07-21 ユーザー要望から発生(時刻修正時のPC TAB操作が手間)。DB変更なし・`DateTimeStepper`部品内部のみ作り替え(props契約不変で3パネル無改修)。`stepLocalSegment`/`setLocalSegment`追加。自動テスト純関数+12件・コンポーネント刷新18件(計432)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)でedit-entry-panel表示+← →キー移動を確認。PR #41(マージ待ち) |
| P5-7 | 実績からの「平日/毎日/複数曜日」まとめ提案(P5-2の拡張。同タイトルが60分以内に揃う3曜日以上を1枚に束ね、weekdays/daily/複数曜日weeklyの定期予定をワンアクション作成。観測曜日のみ定期化) | P2 | P5-2 | 完了 | [specs/P5-7](specs/P5-7_平日毎日まとめ提案.md) | 完了(2026-07-23)。PR #42マージ済み。2026-07-23 ユーザー質問「月〜金のような繰り返しは提案されるか」から発生。仕様承認済み(2026-07-23)。DB変更なし(`computeSuggestions`拡張+既存Action再利用)。束ね閾値=3曜日以上・除外は日単位・「この週に追加」は各日ぶんで部分失敗対応・pattern判定は最終曜日集合から(束ね後の再分割なし)。自動テスト単体/コンポーネント+22件(計454)・結合+2件(計6)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)でM1〜M3手動疎通済み(「毎週平日/毎週月・水・金曜」表示、「毎週にする」でweekdaysルール作成→再提案されないこと確認)。残タスクなし |
| P5-8 | 予定単位の「ズレ%・開始遅延」可視化(サマリーの予定ごとの内訳に、予定比のズレ%と「予定開始に対する実着手の早遅」を表示。P5-3で将来検討として見送った2項目) | P2 | P3-2, P5-3 | 完了 | [specs/P5-8](specs/P5-8_予定単位のズレと開始遅延.md) | 完了(2026-07-25)。PR #44マージ済み。2026-07-25 「他に実装する内容の調査提案」から発生。核心価値「見積もりが当たるようになる」を予定粒度まで下ろす候補①をユーザー選択。仕様承認済み(2026-07-25)。DB変更なし(`GapSummaryItem`に`gapPercent`/`startDelayMinutes`追加+表示のみ)。開始遅延=予定に紐づく実績のうち最早開始−予定開始(正=遅れ/負=早着手/未着手=非表示)。自動テスト単体/コンポーネント+12件(計466)・結合+1件(計88)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)で「-5分(-17%)/着手 予定より 3分早い」「+22分(+37%)/着手 予定より +14分遅れ」「未着手は遅延・%非表示」を手動疎通済み(横スクロールなし)。残タスクなし |
| P5-9 | サマリーの任意期間表示(今日/今週/今月/カスタムの期間選択+← →での前後移動。2日以上の期間は予定・割り込みを同タイトルで集約表示) | P2 | P3-2, P5-8 | 完了 | [specs/P5-9](specs/P5-9_サマリーの任意期間表示.md) | 完了(2026-08-01)。PR #46マージ済み。2026-08-01 ユーザー要望「サマリーはユーザの任意の期間で表示できるようにしたい」から発生。仕様承認済み(2026-08-01)。DB変更なし。上限366日・集計対象は既存踏襲(開始時刻が期間内)・定期予定は選択期間ぶんも冪等実体化。`lib/summary/range.ts`新設、`fetchSyncedEventsInRange`/`fetchTimeEntriesInRange`新設で既存関数はラッパ化(カレンダー・計測画面は無改修)、`materializeRecurringInstances`に期間引数追加(省略時は従来どおり)。自動テスト単体/コンポーネント+42件(計508)・結合+5件(計93)全合格、`npm run check`合格、Playwright 375px(ライト/ダーク)でM1〜M5手動疎通済み(カスタム7月の集約「コードレビュー13件 +3時間41分(+28%)/着手 予定より平均 +8分遅れ」、367日でエラー文言のみ、未実体化の過去期間=2026年5月を表示するだけで定期予定21件が実体化され集計に載ることを確認。全画面で横スクロールなし)。残タスクなし |

## Phase 6 — パフォーマンス改善(2026-08-06 ユーザー要望で追加)

「インフラのプランを課金する以外でアプリのパフォーマンスを向上させたい」という要望から、
RSCのデータ取得経路・DBスキーマ/クエリ・クライアントバンドル/レンダリングを全体調査して発生。
土台は良好(`select('*')`ゼロ、RLSは全ポリシー`(select auth.uid())`ラップ済み、1秒tickは
`useSyncExternalStore`で2つの葉に隔離済み)だが、4領域に明確な無駄が残っていた。
**P6-0は性能ではなく正しさの問題**のため最優先で先に修正する。
P6-1〜P6-3は互いに独立(触るファイルが重ならない)ため、承認後は並行して進められる。

Google連携はフラグOFF(凍結中)のため、同期経路の負債(P6-4)は現時点では発火しない。凍結解除後に効く。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P6-0 | サマリー長期間集計の行数上限バグ修正(PostgRESTの`max_rows`により366日レンジで集計値が黙って過小になる。ページング取得へ) | P1 | P5-9 | 完了 | [specs/P6-0](specs/P6-0_サマリー集計の行数上限.md) | 完了(2026-08-07)。PR #48マージ済み。2026-08-06 パフォーマンス調査中に発見。仕様承認済み(2026-08-06)。**性能ではなく正しさの問題**のためPhase 6の先頭に置く。DB変更なし。`lib/errors/row-limit.ts`・`lib/supabase/paged-fetch.ts`新設、`fetchSyncedEventsInRange`/`fetchTimeEntriesInRange`を`.range()`ループへ。**実DBでRed→Greenを実証**(修正前は1200件投入で1000件しか返らず、合計予定時間が72000分→60000分と過小になることを確認)。実装中に`.order("start_at")`だけではページ間で行の重複・欠落が起きる欠陥を発見し、第2ソートキーに`id`を追加。自動テスト単体/コンポーネント+20件(計528)・結合+6件(計99)全合格、`npm run check`合格。UI変更は集計不能時のメッセージ追加のみ。**残: 本番Supabaseの`Max rows`実値の確認(ユーザー作業。`PAGE_SIZE=1000`が実値を上回ると取りこぼす)** |
| P6-1 | サーバー往復の集約(`auth.getUser()`の重複排除・直列ウォーターフォール解消・`materializeRecurringInstances`の軽量化) | P2 | — | 完了 | [specs/P6-1](specs/P6-1_サーバー往復の集約.md) | 完了(2026-08-07)。PR #49マージ済み。仕様承認済み(2026-08-07)。DB変更なし。**実測でReact 19.2.4の`cache()`はリクエストコンテキスト外では素通し(メモ化しない・例外も投げない)と判明**したため、重複排除は`cache()`単独ではなくWeakMapによるクライアントインスタンス単位のメモ化で実装(vitestで検証可能にするため)。`lib/supabase/session-user.ts`新設(`next/headers`に依存しないよう`server.ts`とは分離)、`getSessionUser`へ12+箇所を置換、`/calendar`の`profiles`取得を実体化と並列化、`recurring_exceptions`に期間フィルタ(前後1日バッファ)追加、`materializeRecurringInstances`の戻り値で`fetchRecurringRules`の二重クエリを解消、`createAdminClient`をlazy singleton化、proxy matcherから`/` `/pricing` `/privacy` `/terms`を除外。**効果を結合テストS16で実測・固定**(実クライアントで`/calendar`相当の3箇所から呼んでも`auth.getUser`は1回)。自動テスト単体/コンポーネント+36件(計564)・結合+4件(計103)全合格、`npm run check`合格(EXIT=0) |
| P6-2 | データ増に耐えるクエリとインデックス(重なり判定を範囲型+GiSTへ、`source`/前方一致用インデックス、RLSポリシーへの`to authenticated`付与) | P2 | P6-0 | 完了 | [specs/P6-2](specs/P6-2_データ増に耐えるクエリとインデックス.md) | 完了(2026-08-07)。PR #50マージ済み。**本番`db push`適用済み(2026-08-07)**。仕様承認済み(2026-08-07)。**マイグレーションあり**。重なり判定が`.lt(start_at).gt(end_at)`のみで`(user_id, start_at)`が片側境界しか使えず、1週間の表示のためにそのユーザーの全履歴をスキャンする構造だった。**当初プランの「最大継続時間を7日と仮定して下限を足す」案は、7日を超える予定が黙って消える(P6-0で直したバグと同種)ため取り下げ**、`tstzrange`生成列+`btree_gist`によるGiSTインデックスに変更(仮定を置かない)。ゼロ長の行が空範囲になって消えるのを`case`で回避。実装中に「生成列の評価がCHECKより先に走り、不正データで22000が23514を覆い隠す」欠陥を実DBで検出し、不正な組み合わせを`null`に倒して修正。効果の実測(26,000行≒3年ぶん): `Seq Scan`/`Rows Removed by Filter: 25496`/2.387ms → `Bitmap Index Scan`/0.090ms(約26倍)。S11として「`Seq Scan`にならないこと」をテストで固定。自動テスト単体+1件(計565)・結合+12件(計115)全合格、`npm run check`合格(EXIT=0)、`db reset`成功・`db diff`差分ゼロ。本番適用後の`db push --dry-run`は`upToDate:true`、`db diff --linked`にP6-2関連の差分なし。残タスクなし |
| P6-3 | クライアントバンドルとレンダリングコストの削減(`date-fns/locale`の`ja` barrel置換、`optimizePackageImports`、`DayColumn`メモ化、編集パネルの遅延ロード) | P2 | — | 完了 | [specs/P6-3](specs/P6-3_クライアントバンドルとレンダリング.md) | `ja` locale barrelが未使用の`formatDistance`/`match`まで取り込みgz約20KB、しかも`/calendar`用と`/track`用でほぼ同一チャンクが2本重複配信。`rangeIsEmpty`が判定のためだけに全日分のレイアウト計算を捨てている。`DayColumn`は`useMemo`/`React.memo`なしで親の任意のstate変更で週7列ぶん再計算。DB変更なし。仕様承認済み(2026-08-08)。**効果の実測**: First Load JS(非圧縮) `/calendar` 668,915→643,392(-25,523/-3.8%)、`/track` 623,451→587,786(-35,665/-5.7%)。71,599バイトの重複チャンク2本は解消。**想定との差**: `ja` barrel置換の効果はraw -5,097に留まった。date-fnsの`format()`が既定ロケール(enUS)を静的importしており`matchPatterns`等が残るため。残り64KBの削減は`format()`の自前実装が必要でスコープ外。**仕様から変えた点**: `now`の`useMemo`固定は現在時刻ラインがハイドレーション時刻で永久固定になるため取りやめ、`actualInputs`の基準時刻を分単位に丸めて参照を安定させた。`useCallback`+`React.memo`は依存の連鎖が1472行全体に及びstale closureリスクに見合わないため見送り(重い計算は`DayColumn`内`useMemo`で停止済み)。既存パネル系テスト13件を`getBy*`→`findBy*`に適合(遅延ロードが実際に非同期になったため)。自動テスト単体/コンポーネント+36件(計601)全合格、結合115件変化なし、`npm run check`合格(EXIT=0)。M1〜M3(375pxライト/ダーク)確認済み(2026-08-08): 全画面で横スクロール0px、`/summary`が`2026年8月8日(土)`と正しく描画(最小localeの実地確認)、stickyバーは`backdropFilter:none`+背景95%不透明+境界線+影で分離維持、遅延ロードされたパネルでフォーカストラップ動作。一時`/api/dev-login`・検証ユーザーは削除済み。PR #52マージ済み(2026-08-08)。**後日対応**: 遅延ロード化で`getBy*`のままのテストが潜在フレークになっており、PR #53のCIで表面化。`next/dynamic`の解決を60ms遅らせる一時構成でフレークを決定的に再現させ、通常実行では出ない6件(`track-view` / `calendar-view-edit`削除フロー / `calendar-view-recurring`「この予定のみ」/ `track-view-running-start` S5)を検出・`findBy*`へ修正(PR #53に同梱)。残タスクなし |
| P6-4 | Google同期経路の往復削減(`router.refresh()`による二重レンダーの解消、`/api/calendar/sync`の同一レンジ二重select統合) | P3 | P6-1 | 未着手(凍結解除待ち) | — | 現在フラグOFFで発火しないが、凍結解除で即座に効く負債。カレンダーを開くたび・週を移動するたびに同期API→`router.refresh()`でRSC全体を再取得し、サーバーレンダーが2回走る。DB変更なし |

| P6-5 | テーブル権限の最小化(`anon`から`public`全テーブルの権限を剥奪、`authenticated`を必要最小限へ、既定権限の停止) | P2 | P6-2 | 完了 | [specs/P6-5](specs/P6-5_anon権限の剥奪.md) | 2026-08-07の本番`db push`時に`db diff --linked`で発見。本番の`anon`に全テーブル(`google_tokens`含む)のDMLが付与されており、さらにローカル・本番とも`anon`/`authenticated`に**TRUNCATE**(RLS対象外)が付いていた。**現時点ではどの経路からも悪用できない**(RLSがポリシーゼロで全拒否・PostgRESTはTRUNCATE非公開・anonにDBログインなし)が、防御が一段減っており既定権限で新規テーブルにも自動付与されるため対応。仕様承認済み(2026-08-08)。**マイグレーションあり**。適用後: `anon`は0件、`authenticated`はprofiles=SELECT,UPDATE/他4テーブル=4種DMLのみ。**実装中の発見**: 既定権限は作成ロールごとに2組(`postgres`所有と`supabase_admin`所有)あり、消せるのは前者のみ(後者はSupabase管理でスコープ外。マイグレーションはpostgresで走るため実害なし)。`service_role`の既定は変更前から`Dxtm`で本変更による影響がないことを`pg_default_acl`の前後実測で確認。CLAUDE.mdのDB/RLS規約に3項目追記。結合+8件(計123)全合格、`db reset`成功・`db diff`差分ゼロ。PR #53マージ済み。**本番`db push`適用済み(2026-08-08)**。適用後の`db diff --linked`検証で`ALTER DEFAULT PRIVILEGES ... ON ROUTINES TO anon/authenticated`の取りこぼし(5-Cの対象がTABLES/SEQUENCESのみだった設計漏れ)を発見し、追補マイグレーション`20260808050650_revoke_routine_default_privileges.sql`で対応(既定権限のみ対象・既存関数は触らない。ローカルは実質no-op、本番でのみ効果)。結合+1件(計124)全合格。PR #54マージ済み・本番適用済み(`db push --dry-run`が`upToDate:true`)。残タスクなし |

### Phase 6 のスコープ外(記録のみ)

| 項目 | 見送り理由 |
|---|---|
| 認証4ページの`supabase-js` 248KB(gz 65KB)削減 | Server Action化で`/login`のFirst Loadを789KB→540KB程度にできるが、認証フロー全体の書き換えになる |
| `components/calendar-view.tsx`(1472行)の分割 | 保守性の課題。性能への寄与はP6-3のメモ化・遅延ロードでほぼ取り切れる |
| `getClaims()`(ローカルJWT検証)への移行 | Supabaseの非対称JWT署名キー有効化(ダッシュボード作業・無料)が前提。P6-1で往復が1回に減った後に再評価する |
| サマリー集計のDB側RPC化 | 正しさはP6-0のページングで担保できる。行転送量の削減は効果を実測してから判断する |
| `synced_events.recurring_rule_id`列の正規化 | `rec:<ruleId>:<date>`の文字列パースをやめる本命の設計だが、既存データの移行を伴う |

## Phase 7 — サマリーの可視化強化(2026-08-09 ユーザー要望で追加)

「サマリー画面でグラフィカルに情報を確認できる案を検討して」から発生。brainstormingで要件確定済み。

サマリーは P3-2 → D-3 → P5-3 → P5-8 → P5-9 → P6-0 と6段階で積み上がり数値表示としては完成しているが、
グラフが1つもない。P5-9 の「スコープ外」筆頭に記録されていた「期間ごとの推移グラフ・チャート表示」
(`specs/P5-9_サマリーの任意期間表示.md:109`)を回収する。

**スコープ判断(ユーザー承認済み)**: 要件定義書 §10 は「傾向分析」をPro/v2に分類しているが、
本Phaseは**既存の集計結果を図にするだけ**で新しい分析・係数学習を一切行わないため、FR-07の拡張としてMVPに含める。
要件定義書は v0.7 で FR-07 に「グラフ可視化」節を追加済み。

P7-1 → P7-2 の順に着手する(両者とも `lib/summary/chart.ts` と `app/(app)/summary/page.tsx` を
触るため並行不可。1本にまとめるとシナリオが50件超になる)。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P7-1 | サマリーの日別ズレグラフ(期間内の日ごとのズレを0基準の発散棒で表示。超過=上向き柿・不足=下向き群青。2日以上の期間のみ) | P2 | P5-9, P6-0 | 完了 | [specs/P7-1](specs/P7-1_サマリーの日別ズレグラフ.md) | 完了(2026-08-09)。PR #56マージ済み。仕様承認済み(2026-08-09)。DB変更なし・新規クエリなし(`page.tsx`が取得済みの`planEvents`/`actualInputs`を使い回す)。`lib/summary/chart.ts`新設(`computeDailyGapSeries`)、`components/summary-daily-gap-chart.tsx`新設、`aggregate.ts`の`durationMinutes`/`isStartInRange`をexport化(挙動不変)。集計規則は`computeGapSummary`と完全一致(開始時刻が期間内・開始日に全量計上)させ、**総和がヒーローの集計値と一致する不変条件をS10でテスト固定**。**チャートライブラリは導入しない**(P6-3のバンドル削減方針)。完全静的でSVG`<title>`+`aria-label`のみ。既存の内訳リストは無改修。結合テストは新規追加せずS10+画面統合テストで代替する判断をユーザー承認済み。**SVG内にテキストを置かない設計**(`preserveAspectRatio="none"`+軸ラベルはHTML)により、日数2〜366のどれでもDOMはrect 1つ/日で済む。自動テスト+36件(計638)全合格・結合124件変化なし、`npm run check`合格(EXIT=0)。**既存4本のsummaryページテストが無改修で通過**(既存リスト無改修の証明)。R-1: UTC/UTC+14/UTC-11の3TZで確認済み。**手動確認で発見・修正**: 0軸線を`stroke-line`にしていたがダークで`--surface`に埋もれ基準線が見えず、`stroke-ink-muted opacity-40`へ変更(ズレ0の中立スタブも`opacity-80`へ)。375px(ライト/ダーク)で週7本・月31日ラベル3個・366日棒366本・today非表示・横スクロール0pxを実測、実データでも不変条件成立(ヒーロー`-0:35` = 日別総和 80+0-130-60+30+45)を確認 |
| P7-2 | サマリーの時間内訳グラフ(実績時間を作業タイトル別に集約し多い順の横棒。予定紐づき=群青・割り込み=柿の積み上げ。上位8件+「その他(M件)」) | P2 | P7-1 | 完了 | [specs/P7-2](specs/P7-2_サマリーの時間内訳グラフ.md) | 完了(2026-08-10)。PR #57マージ済み。仕様承認済み(2026-08-10)。DB変更なし・新規クエリなし。`lib/summary/chart.ts`に`computeActualBreakdown`を追加、`components/summary-actual-breakdown.tsx`新設。`summary.items`/`summary.interruptions`から導出でき**生データ不要**(P7-1と違い日付が要らないため)。同名の予定+割り込みは1行に統合し積み上げ表示(「何に時間を使ったか」が目的のため同一作業名を2行に割らない)。棒の正規化は`maxMinutes`基準・**丸め込み後**に算出(「その他」が最大になり100%を超える罠。S14で回帰固定)。畳む対象が1件なら丸めない(S12)。`range=today`でも表示する(単日でも意味を持つため)。棒のSVGは`aria-hidden`で二重読み上げを回避し、内訳は常にテキストでも出す。自動テスト+34件(計672)全合格・結合124件変化なし、`npm run check`合格(EXIT=0)。R-1: UTC/UTC+14/UTC-11の3TZで確認済み。**既存テスト1件を修正**: `page-range.test.tsx` S36 が`getAllByText`でページ全体を数えており新セクションの行も拾ったため、`within(予定ごとの内訳セクション)`に絞った(挙動の回帰ではなくクエリが広すぎたケース。production変更なし)。375px(ライト/ダーク)で9行への丸め・「実装」の統合(3時間10分=予定2時間30分+割り込み40分、積み上げ78.95%+21.05%=100%)・空状態・横スクロール0pxを実測、内訳合計740分がヒーロー実績12:20と一致することを確認 |

## Phase 8 — 日付またぎの自動反映(2026-08-11 ユーザー報告で追加)

「日にちをまたぐと自動で次の日の表示にしたい」から発生。

原因は2つ重なっている。(1) `CalendarView` が「今日」「現在時刻」をレンダー中の素の `new Date()` で
評価しており、時刻の再レンダートリガーがアプリ内に存在しない(`setInterval` は
`use-now-seconds.ts` の1箇所のみで `RunningTimerBar` 内部に閉じている。`visibilitychange` /
`focus` のリスナは全画面でゼロ件)。(2) `buildCalendarPath` が常に `date` を付けるため、
「今日」ボタンや週ストリップのタップで日付がURLに焼き込まれ、(1) を直しても切り替わらない。

**スコープ判断(ユーザー承認済み)**: `/calendar` のみを対象とする。`/track` と `/summary` にも
同種の問題があるが、`/summary` はサーバーTZ評価との非対称性の是正がセットで必要なため分離した(P8-2 / P8-3)。

**2026-08-12、P8-1 → P8-2 → P8-3 の順で全項目完了**(PR #61/#63/#65マージ済み)。
`/calendar` `/track` `/summary` すべてで「今日」がクライアント(訪問者)のタイムゾーンを
基準に一貫して計算されるようになった。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P8-1 | カレンダーの日付またぎ自動反映(今日を表示中に0時をまたぐと翌日へ追従。現在時刻ラインも1分ごとに追従) | P2 | — | 完了 | [specs/P8-1](specs/P8-1_日付またぎの自動反映.md) | 完了(2026-08-11)。PR #61マージ済み。仕様承認済み(2026-08-11)。DB変更なし。`lib/time/use-now-minute.ts`新設(分単位の共有ストア。**分境界にアラインした`setTimeout`連鎖**で0時を秒単位の遅れで拾い、`visibilitychange`/`focus`/`pageshow`でも再計算)。`buildCalendarPath`に`today`引数を追加し**「URLの`date`は今日以外を見ているときだけ持つ」不変条件**にしたことで、前回の「今日」を保持する ref が不要になった。`calendar-view.tsx`のローカル`useHydrated`は`useNowMinuteMs() !== null`と重複するため削除。日付キー変化時のみ`router.refresh()`を1回(props のイベント・実績が前日基準のままになるため)。**実装の穴をテストで検出**: 購読が途切れている間モジュールスコープの`snapshot`が古いまま止まり、マウント直後の初回レンダーの訂正が「時刻が変わった」ように見えて`refresh`が誤発火(S18がRed)→ `getSnapshot`を「購読者0のときは現在時刻から作り直す」形にして解消。自動テスト+19件(計693)全合格・結合124件変化なし、`npm run check`合格(EXIT=0)。R-1: UTC/UTC+14で確認済み。**既存テスト2件を仕様変更に伴い更新**(S8ビュー切替・S9今日ボタンの遷移先から`date`が消える)。手動確認(Playwright `page.clock`、375px ライト/ダーク): 23:59:30→60秒で`8月11日(火)`→`8月12日(水)`に追従、`?date=`固定時は不変、タイマーを進めず時計だけ10:30→13:12へ飛ばすと`visibilitychange`で即追いつく、横スクロール0px |
| P8-2 | 計測画面の日付またぎ(`entry-groups.ts`の「今日」判定が固定され、0時通過後に当日の実績が未来日扱いで除外されうる) | P2 | P8-1 | 完了 | [specs/P8-2](specs/P8-2_計測画面の日付またぎ.md) | 完了(2026-08-11)。PR #63マージ済み。仕様承認済み(2026-08-11)。DB変更なし。原因はP8-1と同じ(`track-view.tsx`のローカル`useHydrated`が`now`をハイドレーション時に1回だけ評価し以後更新しない)。P8-1で新設した共有ストア`lib/time/use-now-minute.ts`の`useNowMinuteMs()`にそのまま置き換え。**`/track`は`dateParam`を持たずサーバーが常に3週間分取得しているため`router.refresh()`は不要**(P8-1より単純)。自動テスト+3件(計696)全合格、`npm run check`合格(EXIT=0)。R-1: UTC/UTC+14で確認済み。手動確認(Playwright `page.clock`、375px ライト/ダーク): 前日22:00〜0:02の予定が0時をまたぎ終了時刻を過ぎると「今の予定から開始」から消える(修正前は`now`凍結で永久に「進行中」表示され続けるバグだった)/0時直後開始の予定・実績が0時通過後に候補・「今日」グループへ正しく現れる/横スクロール0px |
| P8-3 | サマリーの日付またぎ+TZ非対称の是正(`app/(app)/summary/page.tsx`の`new Date()`が**サーバーTZ**評価で、カレンダー/計測のクライアントTZ判定と非対称) | P2 | P8-1 | 完了 | [specs/P8-3](specs/P8-3_サマリーの日付またぎとTZ非対称の是正.md) | 完了(2026-08-12)。PR #65マージ済み。仕様承認済み(2026-08-12)。DB変更なし。**スコープはユーザー確認済み**: (1) ライブ追従は不要、開き直せば正しければ十分 (2) クライアントのIANA TZをCookieに保存しサーバーが読む方式。`lib/time/now-in-timezone.ts`新設(`resolveNowInTimezone`。TZDateは不正なTZ名でも例外を投げずInvalid Dateになるため`getTime()`のNaN判定で明示フォールバック)。`lib/time/timezone-cookie.ts`(定数・検証。クライアント安全)と`lib/time/timezone-cookie.server.ts`(`readTimezoneCookie`。`next/headers`依存)に分離(1ファイルで実装したところnext buildでクライアントバンドル混入エラーが出たため分離)。`components/timezone-sync.tsx`新設(非表示の葉コンポーネント。Cookie値と検出TZが一致するときは書き込み・refreshともに行わず無限ループを防ぐ)、`app/(app)/layout.tsx`に1つだけ配置。`parseDateParam`に後方互換の第2引数(`referenceZero`)を追加、`resolveSummaryRange`は外部シグネチャ不変のまま`now instanceof TZDate`で内部の暦日解釈を切り替え。**実装前にdate-fns v4 + @date-fns/tzのTZDateがstartOfDay/startOfWeek/addWeeks等を通してもタイムゾーンを保持し、プロセスのTZ環境変数に依存しないことを使い捨てスクリプトで実地検証**(このリポジトリの既知の注意点「TZDateのコピーコンストラクタはTZを引き継がない」を鵜呑みにせず確認)。自動テスト+19件(計715)全合格・結合124件変化なし、`npm run check`合格(EXIT=0)。R-1: UTC/UTC+14で対象8ファイル49件確認済み。既存5本のサマリーページテスト+layout.test.tsxに新規依存のモック追加が必要(無効化ではなく配線)。**手動確認で発見**: `TZDate.toISOString()`はオフセット付き文字列を返しZ正規化とは表記が異なる(瞬時値は等価、Postgresのtstzrangeリテラルとしても正しく解釈されることをローカルDBで確認)。テストのアサーションを文字列完全一致から`getTime()`比較に修正。手動確認(Playwright+CDP `Emulation.setTimezoneOverride`、375px ライト/ダーク): ブラウザTZをPacific/KiritimatiにエミュレートしCookie自動同期・`/summary`の「今日」がそのTZ基準になることを確認、横スクロール0px。**スコープ外として明記**: `buildSummaryPath`の「今日はdateを付けない」不変条件(P8-1と同種)は主要導線が既にパラメータなしのため実害限定的と判断し見送り |

## Phase 9 — 時刻編集UXのモバイル最適化(2026-08-12 ユーザー報告で追加)

「スマホ版だと時刻の修正がやりづらい。PC版と異なる方法を取るかなどUX向上を検討して」から発生。

実績・予定の開始/終了時刻編集は `components/date-time-stepper.tsx`(`DateTimeStepper`)に統一されており、
`EditEntryPanel` / `EditStartPanel` / `AppEventPanel` の3箇所が使う。この入力はP5-6でPCの物理キーボード
(← → で桁移動、↑ ↓ で増減、数字キーで直接入力)向けに設計されており、値セグメントの `<input>` が
`readOnly` のため**モバイルでは仮想キーボードが出ず、共有▲▼ボタンを1ずつタップする以外に値を
変える手段がない**(例: 分を`:00`→`:47`にするには▼を47回タップ)。PC/モバイルの分岐は存在しない。

**方向性(ユーザー承認済み・plan mode)**: モバイル(`matchMedia("(min-width: 1024px)")`未満)では
`<input type="datetime-local">` に切り替え、OS標準のホイールピッカーに任せる。PC版は現行の
`DateTimeStepper`(セグメント入力)を維持する。`DateTimeStepper`の`value`/`onChange`契約が既に
`LOCAL_DATE_TIME_FORMAT`(`"yyyy-MM-dd'T'HH:mm"`)で`datetime-local`のvalue形式と一致するため、
呼び出し元3コンポーネントは無改修で済む見込み。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 備考 |
|---|---|---|---|---|---|---|
| P9-1 | 時刻編集のモバイル最適化(`DateTimeStepper`をモバイルでは`<input type="datetime-local">`に切替。デスクトップ判定は`calendar-context-panel.tsx`の`matchMedia`パターンを共有フック化して流用) | P2 | — | 完了 | [specs/P9-1](specs/P9-1_時刻編集のモバイル最適化.md) | 完了(2026-08-12)。仕様承認済み(2026-08-12)。DB変更なし。`lib/ui/use-is-desktop.ts`新設(`calendar-context-panel.tsx`にあった`matchMedia`キャッシュロジックを移設。挙動不変・既存S14で回帰確認)。`DateTimeStepper`に`useIsDesktop()`分岐を追加し、モバイルでは`<input type="datetime-local">`(単一フィールド、▲▼・カレンダーボタン・セグメントは非表示)、デスクトップは従来のセグメントUIを維持。`value`/`onChange`が既存の`LOCAL_DATE_TIME_FORMAT`文字列と完全一致するため呼び出し元3コンポーネント(`EditEntryPanel`/`EditStartPanel`/`AppEventPanel`)は無改修。**発見**: `tests/helpers/date-time-stepper.ts`を経由する7ファイルがセグメントUI(spinbutton)を前提に操作しており、モバイル分岐追加でjsdomの`matchMedia`未実装状態(既定でモバイル扱いになる)により全滅する経路を実装前に特定。`tests/helpers/match-media.ts`新設(`.matches`をgetterにしたモック。P6-3由来のモジュールスコープキャッシュがあっても後から真偽を切替可能)し、共有ヘルパー`date-time-stepper.ts`のimport時点でデスクトップを既定にすることで7ファイルとも無改修のまま通過を確認。自動テスト+9件(計724)全合格、`npm run check`合格(EXIT=0)。R-1: UTC/UTC+14で影響9ファイル65件確認済み。手動確認: 認証・DBが絡まない一時プレビュールート(確認後削除)で`window.matchMedia`をスクリプトで固定し、モバイル/デスクトップ分岐・ライト/ダーク・`EditEntryPanel`/`AppEventPanel`双方を目視確認(この自動化ブラウザは実ウィンドウを375pxへ縮小できないため、ロジックの分岐自体をforceMobileクエリで確認する方式を採用) |

## Phase 10 — 提案の学習補正(2026-08-13 ユーザー要望で追加)

「提案で入れた予定は計測を継続していくことで再提案されますか?」という質問から発生。
現状は「毎週にする」で定期予定として受け入れた後、実績とのズレが自動で補正される仕組みが
ないことが判明。要件定義書 v2目玉その1「タスク種別ごとの見積もり係数学習」の前倒し・具体化
(P5-2/P5-7がP5-2として実績からの予定提案そのものを前倒ししたのと同じパターン)として、
brainstormingで要件確定・仕様承認済み(2026-08-13)。

| ID | 項目 | 優先度 | 依存 | 状態 | 仕様書 | 対応FR |
|---|---|---|---|---|---|---|
| P10-1 | 提案経由の定期予定の自動学習補正(「毎週にする」で受け入れた定期予定を、紐づく実績の中央値に基づき開始時刻・所要時間を自動調整。最低3サンプル・7日おき再学習・15分未満は無視。停止トグルあり) | v2前倒し | P5-1, P5-2, P5-7 | 完了 | [specs/P10-1](specs/P10-1_提案経由予定の学習補正.md) | FR-13拡張 |

完了(2026-08-13)。マイグレーションあり(`recurring_rules` に `origin`/`last_learned_at` 列を追加。
新規GRANT不要)。`lib/calendar/rule-learning.ts` 新設(`computeRuleAdjustments`/`applyRuleAdjustments`/
`disableRuleLearning`)。`lib/timer/entries.ts` に `fetchRuleLearningEntries` 新設(現在時刻基準で
直近43日の実績を取得。`fetchSuggestionSourceEntries` とは基準日が異なるため別関数)。
`plan-suggestions.tsx` の `makeWeekly` に `origin: "suggestion"` を追加。定期予定編集パネルに
「自動調整を止める」トグル、カレンダー画面に調整のお知らせ帯を追加。自動テスト単体+16件
(rule-learning 12件・rule-adjustment-banner 2件・recurring-rule-panel 4件のうち新規分)・
結合+9件(計742/133)全合格、`npm run check`合格(EXIT=0)。**実装中に発見**: 学習補正後に
同一リクエスト内で `materializeRecurringInstances` を再実行して再実体化する設計だったが、
Next.jsの `fetch` リクエストメモ化により `recurring_rules` への同一SELECTが更新前の内容を
返し続け、実績ベースの新しい時刻がその回のロードでは画面に反映されない不具合を実機確認で発見。
`materializeRecurringInstances` に `prefetchedRules` 引数を追加し、再実体化時はDBを読み直さず
学習補正で判明済みの値をそのまま渡す設計に変更して解消(副次的に無駄な重複クエリも削減)。
手動確認(Playwright、375px ライト/ダーク): ローカルSupabaseに提案経由ルール+実績を直接投入し、
`/calendar` を開くと開始時刻・所要時間が実績にあわせて自動調整され、お知らせ帯
「『朝会』を実績にあわせて調整しました(開始 09:00→09:15・所要 30分→45分)」が表示されること、
定期予定編集パネルの「自動調整を止める」トグルをONにすると `origin` が `manual` に変わり
以降調整対象から外れることを確認。横スクロールなし。

## v2以降 — 先回り実装禁止(記録のみ)

要件定義書 §4.2 より。優先順は将来ユーザーと再検討する。

| 項目 | 位置づけ |
|---|---|
| タスク種別ごとの見積もり係数学習・計画時サジェスト | **v2の目玉その1** → ユーザー要望により**P5-2として前倒し**(2026-07-14)。さらに提案経由の定期予定の自己補正部分を**P10-1として前倒し**(2026-08-13) |
| GitHub / Linear / Jira 連携(ブランチ・Issueから記録自動化) | **v2の目玉その2** |
| 集中時間(メーカータイム)分析・会議コスト可視化 | Pro機能候補 |
| 日報・スタンドアップ報告の自動生成 | |
| 履歴無制限などのPro課金実装 | 料金ページの計測結果を見て判断 |
| 複数カレンダー、Google以外(Outlook等) | |
| Googleカレンダーへの書き戻し(writeスコープ) | **OAuth審査要件が変わるため人間が判断**(CLAUDE.md) |
| キーボードショートカット、CLI、Raycast拡張、API公開 | |
| レポート出力(CSV/PDF)、請求書機能 | |
| ネイティブアプリ、ウィジェット、プッシュ通知 | Expo / Capacitor 候補 |
| チーム機能、Togglインポート | |

---

## 次に着手(推奨順)

**2026-08-13時点、P10-1(提案経由の定期予定の自動学習補正)が完了**。着手待ちキューは再び空。
Phase 10 はDB変更を含む(`recurring_rules` へ `origin`/`last_learned_at` 列を追加)ため、
**本番Supabaseへの `db push` が必要**(ユーザー作業)。

**2026-08-12時点でコード側の着手待ちキューは再び空**。Phase 9(時刻編集のモバイル最適化)は
P9-1が完了(DB変更なし)。Phase 8(日付またぎ)は
P8-1 / P8-2 / P8-3 とも完了(PR #61/#63/#65マージ済み)。**Phase 8・9 はDB変更を含まないため
`db push` は不要**。
Phase 7(サマリーの可視化強化)は P7-1 / P7-2 とも完了(PR #56/#57マージ済み)。
Phase 6 は P6-4(凍結解除待ち)を除き全完了、本番DBも `db push --dry-run` が `upToDate:true`。
**Phase 7 はDB変更を含まないため、本番への `db push` は不要**。
同日、**P3-3(PWA対応)もM2の実機確認合格で完了**(PR #59)。長く保留になっていた実機確認が片付き、
Phase 3 は P3-1 / P3-2 / P3-3 の全項目が完了した。

次は下記のいずれかをユーザーと決めて着手する。

1. **P4-6(ベータユーザー投入)**: Phase 4の実装項目(P4-1〜P4-5)が全完了、Phase 5・Phase 7も完了。
   サマリーの可視化が揃い、コア体験(計画→記録→ギャップ→振り返り)は一通り成立している。
   募集チャネル(要件定義書 §14)が未決のため、着手前にユーザーと方針確認が必要
2. **Google連携の凍結解除**: 解除するなら P0-5(OAuth審査申請)→ P6-4(同期経路の往復削減)、
   および P1-2 の残(M2=Google側の予定変更が「更新」で反映されること、Vercel環境変数追加)、
   P1-3 の残(Google Cloud Consoleへのコールバック登録、連携実機疎通M3)がセットで動く。
   審査のリードタイムが長いため、判断は早いほうがよい
3. **新規の機能拡張**: Phase 5 と同様、ユーザー要望または調査提案から起こす
4. **ユーザー作業(並行可・実装をブロックしない)**:
   - 本番Supabaseの `Max rows` 実値の確認(P6-0の前提。`PAGE_SIZE=1000`が実値を上回ると取りこぼす)
   - 本番Supabaseの認証設定: Redirect URLに`/auth/confirm`・`/auth/confirm-recovery`を追加
     (未設定だとメール確認・パスワードリセットのリンクが機能しない)+メール確認必須化(P1-3残)
5. **経過観察(着手項目ではない)**: P3-3 の M2-b/c(PWAを1時間以上・数日空けて再起動しても
   ログイン状態が維持されること)は時間経過が必要なため未確認のまま完了とした。
   再発した場合は新規項目として起票する([specs/P3-3](specs/P3-3_PWA対応.md))

> 判断メモ(2026-07-09更新): ドメイン取得・OAuth審査のリードタイムが長期化し開発のボトルネックに
> なっていたため、Googleに依存しないメール認証を主要導線として先に成立させる方針に転換した
> (要件定義書 §12のリスクを踏まえた優先順位変更。ユーザー承認済み)。P1-3は2026-07-09に実装完了。
> OAuth審査自体はGoogle連携機能の一部として引き続き必要なため取り下げず、次点で着手する。

> 判断メモ(2026-07-10更新): 方針転換を一歩進め、Google連携UIを環境フラグで当面凍結し、
> 予定はアプリ内で作成・編集・削除できるようにする(P2-5。ユーザー承認済み)。これにより
> Google非依存でコアループ(計画→記録→ギャップ)が完結する。連携コード・テストは削除せず、
> フラグONで復活可能な状態を維持する。P0-5(OAuth審査)は凍結解除まで保留。
