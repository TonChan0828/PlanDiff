# 2026-07-09

## P3-3: PWA対応(仕様書承認→TDD実装)

- やったこと:
  - ユーザーから仕様書`docs/specs/P3-3_PWA対応.md`の承認を受領(2026-07-08付、日をまたいで実装着手)。ステータスを「承認済み」に更新
  - テストシナリオS1〜S7をRed→Green:
    - `lib/pwa/theme.ts`(新規): ブランドカラー(`#0284c7`)とダーク背景色の定数
    - `lib/pwa/app-icon.tsx`(新規): `next/og`の`ImageResponse`でブランドカラー背景+白文字「P」のアイコンを生成する共通関数`generateAppIcon(sizePx)`
    - `app/manifest.ts`(新規): name/short_name/start_url/display/theme_color/icons(192・512・maskable)

## トラブル: npm run buildとdevサーバーの.next競合によるキャッシュ破損(2回)

- やったこと: `npm run check`内の`npm run build`実行時、ユーザーが起動していたdevサーバー(port 3000)とTurbopackキャッシュ(`.next/dev/cache/turbopack`)が競合し破損、全ルートが500エラーに。`.next/dev/cache/turbopack`のみ削除してdevサーバーを再起動したが、直後に`Cannot find module '../chunks/ssr/[turbopack]_runtime.js'`で再度破損(dev/buildで`.next`全体を共有していたことが原因と判断)。`.next`ディレクトリ全体を削除してdevサーバーを再起動し復旧
- 結果: 復旧確認(`/`・`/manifest.webmanifest`・`/icon-192`・`/apple-icon`が200)。ユーザーの元のdevサーバープロセスを2回killすることになった(いずれもキャッシュ破損で既に応答不能だったもの)
- 未解決・次にやること: 今後`npm run build`を実行する際は、ユーザーのdevサーバーと`.next`を共有しない([別ディレクトリでのbuild、または実行前にdevサーバー停止を依頼]を検討)。再発時は同じ手順(`.next`全削除→`npm run dev`再起動)で対応可能

## 調査: 実機(スマホ)でログインボタンが反応しない

- やったこと: ユーザーがM2確認のためスマホで`http://192.168.11.4:3000/login`にアクセスしたところ、ログインボタンをタップしても無反応(画面遷移もエラーもなし)と報告。プライベートブラウジングタブでも同じ症状が再現し、キャッシュ由来ではないと判明。JSチャンクの配信状況(`curl`で全チャンク200を確認)、`.env.local`の内容を調査した結果、`NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`(ローカルSupabase、ループバックアドレス)がクライアントバンドルに埋め込まれていることが原因と特定。ビルド時にNode.js環境変数がバンドルへ静的に埋め込まれるため、スマホ(別デバイス)からアクセスした場合、ログインボタン押下後のリダイレクト先(Supabaseの認可URL)がスマホ自身のループバックを指してしまい遷移が成立しない
- 結果: P3-3(PWA)の実装起因ではなく、「ローカルSupabase(127.0.0.1)を使う開発サーバーに外部の実機からアクセスした」ことによる既知の環境制約と判断。これまでの手動疎通(P2-1〜P3-2)は同一マシン上のPlaywrightで実施していたため表面化していなかった
- 未解決・次にやること: M2の実施方法をユーザーに確認中(Vercel Preview経由 / 本番URL代替 / ローカルSupabaseのネットワーク公開、の3案を提示)。ユーザーからは「一旦スルー」の指示を受け、M2は保留のまま次に進める。P3-3は「実装中」のまま(自動テスト・build・M1は合格済み、M2のみ保留)

## PR作成(P3-3)

- やったこと: ユーザー指示「コミット PR作成」。`feat/p3-3-pwa`ブランチを作成し、実装・テスト・仕様書・開発計画・ログ(18ファイル)をコミット(pre-commitフック全項目合格、Prettier警告を1件修正して再コミット)。push、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/10(Vercel Previewデプロイが走る想定。M2はPreview URLでの確認を検討中)
- 未解決・次にやること: レビュー・マージ待ち。マージ後はローカルmainをorigin/mainに追従。M2(実機確認)の進め方は引き続き保留

## M2をVercel Previewで確認する方針に決定

- やったこと: ユーザー指示「M2はVercel Previewで確認する」を受け、PR #10のVercel Previewデプロイ状況を確認。`gh pr view 10`のコメントからPreview URL(`https://plandiff-git-feat-p3-3-pwa-tonchans-projects.vercel.app`)を取得。`curl`で疎通確認したところ全ルートが302で`vercel.com/sso-api`にリダイレクトされることが判明(Vercelプロジェクトの Deployment Protection(SSO)が有効なため)。ユーザーに、スマホでVercelアカウントにログイン済みならSSO画面を通過してアクセスできる旨と、通過できない場合はDeployment Protectionの無効化(ユーザー判断・ダッシュボード操作)が必要な旨を案内
- 結果: M2の確認手段としてVercel Previewを使う方針を確定。SSO通過可否はユーザー確認待ち
- 未解決・次にやること: ユーザーがスマホでPreview URLにアクセスしSSO通過・ホーム画面追加・操作確認(M2)を実施

## PR #10マージ → main反映確認

- やったこと: ユーザーからPR #10マージ完了の報告を受領。ローカルmainをorigin/main(マージ済み、fast-forward)に追従。マージ済みのfeat/p3-3-pwaブランチをローカルから削除。mainへのマージpushで走ったCIが`success`になったことを確認
- 結果: P3-3の実装がmainに反映済み。CI成功確認済み
- 未解決・次にやること: M2(実機でのVercel Preview経由の確認)がユーザー確認待ち。合格後にP3-3を開発計画で「完了」に更新

## P1-3: メール認証とGoogle任意連携(方針転換→仕様書承認→TDD実装)

- やったこと:
  - ユーザーから方針転換の相談: 独自ドメイン取得・Google OAuth審査(P0-5)が停滞し開発が進まないため、
    「Google審査回避」と「Googleカレンダー非依存の価値提供」の両方を狙いとして、メール/パスワード
    認証を主要導線に据え、Googleカレンダー連携をログイン後の任意機能に切り離す方針を検討。
    brainstorming(認証方式の共存/メイン化、メール認証方式、メール確認必須化、パスワードリセット
    有無、`/calendar`のGoogle未連携時UX、Google連携の実装方式=Supabase linkIdentityか独自OAuth2か、
    優先順位)を1問ずつ確認し、すべてユーザー承認済みの設計としてまとめた
  - `docs/specs/P1-3_メール認証とGoogle任意連携.md`を新規作成(目的・画面設計・各フロー・エラー
    挙動一覧・テストシナリオS1〜S26+手動疎通M1〜M4)。`docs/specs/P1-1_Google認証.md`に後継注記を
    追記。`docs/要件定義書.md`のFR-01/FR-02を改定。`docs/開発計画.md`にP1-3を最優先(P0-5より前)
    として追加。ユーザーから仕様書承認を受領(2026-07-09)
  - TDDで実装(Red→Green→Refactor、サブエリアごとに`npm run check`):
    - メール/パスワード認証コア: `lib/auth/validation.ts`・`lib/auth/messages.ts`(新規)、
      `components/signup-form.tsx`・`components/login-form.tsx`(新規)、`app/(marketing)/signup/page.tsx`
      (新規)、`app/(marketing)/login/page.tsx`(書き換え、Googleボタン廃止)、`app/auth/confirm/route.ts`
      (新規、`verifyOtp`によるsignup/recovery確認リンク検証)
    - パスワードリセット: `components/forgot-password-form.tsx`・`components/reset-password-form.tsx`、
      `app/(marketing)/forgot-password/page.tsx`・`app/(marketing)/reset-password/page.tsx`(いずれも新規)
    - Google連携(Supabase Authから独立したOAuth2フロー): `lib/google/connect-options.ts`(新規)、
      `lib/google/token.ts`に`exchangeAuthorizationCode`追加、`app/api/google/connect/route.ts`・
      `app/api/google/callback/route.ts`(新規、CSRF `state`をhttpOnly cookieで検証)
    - `/calendar`のGoogle未連携UX: `app/api/calendar/sync/route.ts`のトークン欠如時レスポンスを
      `reauthorize`→`not_connected`に変更(連携失効とは区別)、`components/calendar-view.tsx`の
      401強制遷移(`router.push`)を撤廃しバナー表示に変更、`components/google-connection-banner.tsx`
      (新規)、`app/(app)/calendar/page.tsx`に`googleConnected`props配線+設定リンク追加
    - 設定画面: `app/(app)/settings/page.tsx`・`app/(app)/settings/actions.ts`(新規、連携状態表示・
      解除)、`lib/settings/messages.ts`(新規)
    - 廃止コード削除: `app/auth/callback/route.ts`・`app/(app)/auth/reauthorize/page.tsx`・
      `components/google-sign-in-button.tsx`・`lib/supabase/auth-options.ts`と対応テスト一式
      (参照なしをgrepで確認済み)
  - 375px確認(Playwright、`npm run dev`): `/signup`・`/login`・`/forgot-password`・`/reset-password`
    (リンク期限切れ状態)・`/settings`の表示崩れなしを確認。さらにローカルSupabase(ローカルは
    `enable_confirmations=false`のため確認メール不要で即ログイン)で実際にサインアップ→`/calendar`
    (Google未接続バナー表示・予定レーンは空・フリータイマー操作可能を実機的に確認)→`/settings`
    (未連携表示・連携するボタン)→`/api/google/connect`(実Google認可URLへのリダイレクトを確認。
    `redirect_uri_mismatch`は想定通り。Google Cloud Console側での新コールバックURL登録が未実施の
    ためで、コード側の不具合ではない)の一連を確認。確認用テストユーザーは作業後に削除済み
- 理由: ドメイン取得・OAuth審査のリードタイムが開発のボトルネックになっており、Google非依存で
  価値提供できる状態を先に成立させるユーザー判断
- 結果:
  - `npm run check`(typecheck+lint+test+build)全件合格。単体テスト171件全件合格。結合テストは
    全8ファイル47件(core-schema.test.ts除く7ファイル37件は複数回実行してすべて安定合格。
    core-schema.test.ts単体実行では常に合格)
  - 結合テスト全体実行時、`core-schema.test.ts`(P0-6、S9/S10)が並列実行時にまれに失敗する
    既存の(P1-3と無関係な)フレーク性を確認。単体実行では常に合格し、P1-3関連の新規結合テスト
    ファイルを除外しても再現するため、本タスク由来ではないテスト基盤側の既知課題と判断。修正は
    スコープ外としユーザーに報告のみ
- 未解決・次にやること:
  - Google Cloud Console側で新コールバックURL(`{origin}/api/google/callback`、ローカル・本番
    両方)をOAuthクライアントの許可リダイレクトURIに追加する(ユーザー作業)。完了後にGoogle連携の
    実機疎通(M3)が可能になる
  - 本番Supabaseプロジェクトでメール確認必須化(`enable_confirmations`)・メールテンプレート
    (`{{ .SiteURL }}/auth/confirm`宛)の設定確認(ユーザー作業。ローカルは`enable_confirmations=false`
    のため未検証)
  - 手動疎通M1(実メールでの確認リンク)・M2(パスワードリセット実メール一連)は本番相当の環境
    でユーザー確認が必要
  - `docs/開発計画.md`のP1-3を「実装中」→完了に更新(本ログ追記と合わせて実施)
  - P0-6の`core-schema.test.ts`並列実行時フレークは別途調査要否をユーザーと相談

## トラブル: P1-3実装直後、実際にはメール確認リンクが機能していなかった(2件のバグ)

- やったこと: ユーザーから「メールが送られてきません」と報告。ローカル環境と判明
  (`supabase/config.toml`の`enable_confirmations=false`によりローカルでは元々メール未送信の
  仕様だったため)。ユーザーが実際にメール確認フローを確認したいとのことで、
  `enable_confirmations=true`に変更しローカルSupabaseを再起動、実メール(Mailpit)経由で
  signup→確認リンククリックの一連を実機的に検証したところ、以下2件の実装バグを発見:
  1. **`supabase/config.toml`の`additional_redirect_urls`が新ルートを許可していなかった**:
     削除済みの`/auth/callback`のみが登録されたままで、新設した`/auth/confirm`が未登録。
     Supabaseは許可リストにない`redirect_to`を要求された場合、エラーを返さず`site_url`へ
     静かにフォールバックするため、確認メールのリンクが常にトップページへの空リダイレクトに
     なっていた(実装時に見落とし。仕様書には「許可リストの更新が必要」と書いていたが
     `config.toml`への反映を怠っていた)
  2. **`/auth/confirm`の検証方式が実際のリンク形式と一致していなかった**: `@supabase/ssr`は
     デフォルトでPKCEフローを使うため、GoTrueの検証後のリダイレクトは`token_hash`ではなく
     認可コード(`?code=...`)で返ってくる。実装時は`verifyOtp({ token_hash, type })`を
     前提にしており(P1-1のGoogle OAuthとは異なるSDK挙動の思い込み)、`code`を受け取っても
     `token_hash`必須のガードで即座に失敗していた
  - 修正: `additional_redirect_urls`に`/auth/confirm`・`/auth/confirm-recovery`(新設。
    signup/recoveryで遷移先が異なるためパスを分離。クエリ文字列でのnext分岐は許可リスト照合で
    無視される可能性があり避けた)を追加。両ルートを`exchangeCodeForSession(code)`ベースに
    書き直し。`components/forgot-password-form.tsx`の`redirectTo`を`/auth/confirm-recovery`に変更
  - 副次的に発見: パスワード再設定成功後、`updateUser`だけではリカバリーセッションが有効なままの
    ため、`/login?reset=success`へ`router.push`しても`/login`側の「ログイン済みなら/calendarへ」
    ガードに即座に弾かれ、成功メッセージが表示されないバグも同時に発見。`updateUser`成功後に
    明示的に`signOut()`してから遷移するよう修正
  - `docs/specs/P1-3_メール認証とGoogle任意連携.md`のメール確認フロー節・テストシナリオ
    (S1/S6/S7/S10/S12)を実際の挙動(`exchangeCodeForSession`・ルート分離)に合わせて修正。
    関連テスト(`tests/app/auth/confirm/route.test.ts`書き換え、
    `tests/app/auth/confirm-recovery/route.test.ts`新規、
    `tests/components/forgot-password-form.test.tsx`・`tests/components/reset-password-form.test.tsx`
    更新)を追随させ、`npm run check`(172件)全件合格を再確認
  - 実メールでの一連を再検証: サインアップ→確認メール受信→リンククリック→`/calendar`で
    実際にログイン状態になることを確認。パスワード再設定→回復メール受信→リンククリック→
    `/reset-password`→新パスワード設定→`/login?reset=success`(成功バナー表示)→新パスワードで
    ログイン→`/calendar`、まで一連が実際に動作することを確認。確認に使ったテストユーザーは削除済み
- 理由: `npm run check`は自動テストで境界を満たすモック前提のため、Supabaseの実際のメール
  テンプレート・許可リスト・PKCE挙動といった「モックしていた前提そのものの誤り」までは検知できない。
  ユーザーからの実地報告がなければ本番でも同じ理由でメール確認が機能しない状態のままリリースされていた
- 結果: `npm run check`全件合格。実メールでのsignup確認・パスワードリセットの一連を実機確認済み
- 未解決・次にやること: 本番Supabaseプロジェクト側でも`additional_redirect_urls`(Site URL設定)に
  本番の`/auth/confirm`・`/auth/confirm-recovery`を登録する必要がある(ローカルの`config.toml`とは
  別に、本番はSupabaseダッシュボードの認証設定で管理されるため、デプロイ時にユーザー側で確認が必要)
