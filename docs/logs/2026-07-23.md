# 2026-07-23

## 22:00 P5-7 実績からの「平日/毎日/複数曜日」まとめ提案

- やったこと:
  - ユーザー質問「月〜金のような繰り返しは提案されるか」を起点に現状(P5-2)を調査。実績のみで提案が
    出ること、フリー/予定連動を区別しないこと、平日毎日は個別曜日に割れて最大3件に埋もれることを確認。
  - 方針をブレインストーム。閾値=3曜日以上・daily対応・「観測曜日のみ定期化」をユーザーと確定。
    `docs/specs/P5-7_平日毎日まとめ提案.md` を作成し承認取得。
  - `computeSuggestions` を拡張: 既存の曜日別クラスタリングの後段に「タイトル単位で開始時刻60分以内に
    揃う3曜日以上を束ねる」処理を追加。`PlanSuggestion` を `weekday/date` → `pattern/weekdays[]/dates[]` へ変更。
    除外は日単位で判定し、残った曜日集合から pattern(daily / weekdays / weekly)を再判定。
  - `messages.ts` に `suggestionPatternLabel` を追加(毎日 / 毎週平日 / 毎週◯・◯曜)。
  - `plan-suggestions.tsx`: まとめカードの「この週に追加」を対象各日ぶんのループ+部分失敗対応に、
    「毎週にする」を pattern 別呼び分け(weekly=weekdays配列 / weekdays・daily=null)に更新。
  - TDD: シナリオ S25〜S41 を追加、既存 S1〜S24 を新shapeへ更新(値・意図は不変)。
- 理由: 記録はあるが計画に載っていない「平日毎日」等の繰り返しを1アクションで定期化できるようにするため
  (FR-13の拡張、P5-2スコープ外欄)。
- 結果:
  - `npm run check`(typecheck+lint+test+build)合格。テスト 454件全合格(単体/コンポーネント+22件)。
  - 結合テスト `test:integration` 6件合格(S20〜S23 + S40 weekdays実体化 + S41 daily実体化)。
  - Playwright 375px(ライト/ダーク)で実機確認: デモユーザーに平日×3週・月水金×3週の実績を投入し、
    「毎週平日 09:00頃・約30分(直近4週で15回)」「毎週月・水・金曜 14:00頃・約45分(直近4週で9回)」の
    まとめカードが表示・折り返しなし・ボタンタップ可を確認。「毎週にする」で `pattern='weekdays'`,
    `weekdays=null`, 09:00-09:30, starts_on=2026-07-27 のルールが作成され、当該カードが再提案されないことを確認(M1〜M3)。
- 未解決・次にやること: なし。PR #42 マージ済み(2026-07-23)。スコープ外(束ね後の再分割はしない=1枚のまま最終曜日集合で表示)は仕様書に明記済み。
