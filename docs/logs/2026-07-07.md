# 行動履歴 2026-07-07

## P2-1: カレンダービューの仕様書作成(承認待ち)

- やったこと:
  - ユーザー指示「次の実装へ」。PR #2(P1-2)マージ済みを確認し、ローカルmainを追従 → `feat/p2-1-calendar-view` ブランチ作成(旧p1-2ブランチ削除)
  - `docs/指示資料/` 確認(READMEのみ、新規資料なし)。FR-03・§6・ui-quality Skill・P1-2実装(calendar-sync.tsx / page.tsx)を確認のうえ `docs/specs/P2-1_カレンダービュー.md` を作成
  - 構成: lib/calendar(view-date / layout / messages)+ components/calendar-view.tsx(日/週タイムライン、URL状態 `?view&date`、P1-2の同期トリガを統合)。テストシナリオ S1〜S15 + 手動M1〜M2
  - 設計判断(承認時に確認): 既定=日ビュー(モバイルファースト)、予定ブロックは左寄せ55%レーン(FR-06オーバーレイ規約の先取り)、重複は横等分割、ビュー状態はURLのみ
- 理由: 開発計画 P2-1(FR-03)。spec-driven-dev ワークフロー(仕様書→承認→TDD)
- 結果: 仕様書ドラフト完成。開発計画のP2-1を「承認待ち」に更新
- 未解決・次にやること:
  - ユーザー承認待ち(既定ビュー・予定レーン幅の2点も確認)
  - P1-2の残タスク(ユーザー作業): 手動疎通M1/M2、Vercelへの `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` 追加

## P2-1: カレンダービューの実装(TDD)と検証

- やったこと:
  - 仕様承認(既定=日ビュー、予定=左55%レーンで確定)を受けて実装
  - Red: S1〜S15を tests/ に展開(単体: lib/calendar/view-date・layout+コンポーネント、結合: tests/integration/calendar-view.test.ts)。実装なしで失敗を確認
  - Green: `lib/calendar/view-date.ts`(URL状態の解釈・週列・ナビ先)、`lib/calendar/layout.ts`(時刻→%配置、日跨ぎクリップ、重複の列詰め、最小高さ24分)、`lib/calendar/messages.ts`(文言集約)、`lib/calendar/events.ts`(page用読み取りヘルパー)、`components/calendar-view.tsx`(日/週タイムライン+週ストリップ+現在時刻ライン+P1-2同期トリガ統合)、page を searchParams(`?view&date`)対応に更新
  - `calendar-sync.tsx` と対応テストは削除(S18シナリオは新テストS7/S11/S12が引き継ぎ。仕様書に補記)
  - SSR/クライアントのTZ不一致対策として、時刻位置・「今日」依存の描画は `useSyncExternalStore` のハイドレーション判定後に行う
  - 375px検証(ui-quality): 検証ユーザー+予定7件(重複・極短・長タイトル・日跨ぎ)を投入し、同期APIをPlaywrightでモックして実表示。日/週(375px)・週(1280px)・ダークで横スクロールなし、ナビゲーション(次へ/前へ)の動作も確認。週ビューの狭列でタイトルが縦流れしたため `line-clamp-2` を追加。検証ユーザー・cookie・スクリーンショットは削除済み
- 理由: 開発計画 P2-1(FR-03)
- 結果: `npm run check` 全通過(単体54件)+ 結合25件全合格(出力確認済み)。全シナリオS1〜S15にテスト対応(単体21+結合2の計23件を新規追加)
- 未解決・次にやること:
  - **ユーザー確認**: M1(実予定が正しい時刻位置に表示)/ M2(週ナビゲーションで前週・翌週の予定が同期表示)。`npm run dev` でGoogleログインして確認
  - M1/M2確認後に開発計画のP2-1を「完了」へ更新。次はP2-2(予定連動タイマー)
  - P1-2から継続のユーザー作業: Vercelへの `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` 追加

## PR作成(P2-1)

- やったこと: ユーザー指示「PR作成」。`feat/p2-1-calendar-view` ブランチ(コミット `fe6eb5f` 1件、main差分)をpush、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/3(ブランチpushによりVercelのPreviewデプロイが走る想定)
- 未解決・次にやること: レビュー・マージ。マージ前チェックはPR本文に記載(手動疎通M1/M2、Vercel環境変数 `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` の追加)

## P2-1: 手動疎通M1/M2合格 → 完了

- やったこと: ユーザーから手動疎通M1(実予定が正しい時刻位置に表示)/ M2(週ナビゲーションで前週・翌週の予定が同期)の合格報告を受領。開発計画のP2-1を「完了」に更新し、「次に着手」をP2-2(予定連動タイマー)に更新
- 補足: P1-2のM1(実予定の表示)も今回の確認で実証されたため備考に反映。P1-2の残はM2(Google側の予定変更→「更新」で反映)とVercel環境変数追加のみ
- 未解決・次にやること: PR #3 のレビュー・マージ(ユーザー)。マージ後にローカルmainを追従。次の実装はP2-2

## 07:53 P2-2: 仕様書作成(予定連動タイマー)

- やったこと: ユーザー指示「次の実装へ」を受け、開発計画の次項目 P2-2(予定連動タイマー)の仕様書 `docs/specs/P2-2_予定連動タイマー.md` を作成。指示資料を確認(READMEのみ、関連資料なし)。開発計画のP2-2を「承認待ち」に更新
- 理由: spec-driven-dev ワークフロー(仕様書→承認→シナリオ→TDD)。スキーマ変更なし(P0-6の `time_entries` + partial unique index をそのまま使用)
- 結果: 仕様書ドラフト完成(単体S1-S2、コンポーネントS3-S8、結合S9-S14、手動M1-M2)。設計判断2点(実績ブロックの最小表示をP2-2に含める/予定タップで即開始)をユーザーに確認予定
- 未解決・次にやること: ユーザー承認 → ブランチ作成(mainへのPR #3マージ状況の確認後)→ Red(テスト展開)

## 21:38 P2-2 実装(予定連動タイマー)TDD完了

- やったこと:
  - 仕様書 P2-2 を承認済みに更新(ユーザー確認: 実績最小表示をP2-2に含める/予定タップで即開始・停止)
  - ブランチ `feat/p2-2-event-timer` を origin/main から作成(P2-1のPR #3はマージ済みを確認、ローカルmainを追従)
  - Red: シナリオS1〜S14をテスト化(lib/timer elapsed・blocks、components calendar-view-timer・running-timer-bar、integration timer-service)
  - Green: `lib/timer/`(types/elapsed/blocks/messages/entries/service)、Server Action `timer-actions.ts`、`running-timer-bar.tsx` を実装。`calendar-view.tsx` を予定ブロックのボタン化+右レーン実績表示+タイマーバー+楽観的更新に拡張。`page.tsx` で実行中エントリ・実績を取得。`layout.ts` をジェネリック化(googleEventId等の追加フィールドを保持)、`events.ts` に google_event_id を追加
  - lintエラー2件(set-state-in-effect)を修正: タイマーバーは useSyncExternalStore で秒更新、props追従は render中の派生state調整に変更
- 理由: FR-04。実行中1本はDBの partial unique index が最終防衛線、開始は停止→insertを競合時1回リトライ。時刻はサーバー決定・UTC保存
- 結果:
  - `npm run check` 合格(typecheck + lint + test 75件 + build すべてOK)
  - 375px手動疎通(Playwright + ローカルSupabase検証データ): 日/週とも横スクロールなし。オーバーレイ(予定=左・薄、実績=右・濃)を確認。予定タップ→開始で既存が自動停止され実行中1本(DBで確認: g-1実行中・g-2にend_at)、タイマーバーに経過時間+停止。検証ユーザー・cookie・スクリーンショットは削除済み
- 未解決・次にやること:
  - M1(実Googleアカウントで開始→リロード復元→停止→実績が右レーンに残る)/ M2(2デバイスで片方の開始がもう片方の自動停止)はユーザーの手動確認待ち(P2-1と同じ運用)。確認後に開発計画のP2-2を「完了」へ
  - スキーマ変更なし(P0-6の time_entries を使用)。コミット→PRはユーザー指示で

## PR作成(P2-2)

- やったこと: ユーザー指示「コミットしてPR作成」。`feat/p2-2-event-timer` ブランチ(コミット1件)をpush、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/4(Vercel Previewデプロイが走る想定)
- 未解決・次にやること: レビュー・マージ。マージ前チェックはPR本文に記載(M1: 実Googleアカウントでの一連操作、M2: 複数デバイスでの自動停止確認)
