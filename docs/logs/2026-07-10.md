# 2026-07-10

## ユーザーからの実機報告(メール確認・アカウント有効化)

- やったこと: ユーザーから「メールがいまだ届かず、アカウントを有効にできません」と再度報告
  (`[実メールアドレス]`)。ローカルSupabase(`npx supabase start`)は実際のメール
  サーバーへ送信せず、Mailpit(ローカルの捕捉ツール、`http://127.0.0.1:54324`)に確認メールを
  溜めるだけであることを確認・説明。ユーザー自身でMailpitを確認する方針となった
- 結果: ユーザーが自身でMailpit経由で確認リンクをクリックし、アカウント有効化を確認
- 未解決・次にやること: 本番環境では実際のメールサーバー経由で届くようになる旨は
  `docs/開発計画.md`のP1-3残タスクに既に記載済み

## ユーザーアカウント削除依頼

- やったこと: ユーザー指示によりローカルSupabaseの`auth.users`から`[実メールアドレス]`の
  アカウントを削除(service role経由)。P0-6のcascade設計により関連する`profiles`等の行も
  自動削除
- 結果: 削除完了

## P1-3コミット・PR作成

- やったこと: ユーザー指示「コミット PR作成」。`feat/p1-3-email-auth`ブランチを作成し、
  P1-3の実装一式(59ファイル。メール/パスワード認証、Google連携の独立OAuth2フロー、
  `/calendar`のGoogle未連携UX、`/settings`、旧Google専用ログインコードの削除、仕様書・
  要件定義書・開発計画・ログの更新)をコミット。pre-commitフック(format/typecheck/lint/test
  172件/build)全項目合格。Prettier警告を1件修正して再コミット。push、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/11
- 未解決・次にやること: レビュー・マージ待ち。マージ後は本番Supabaseの認証設定
  (Redirect URLに`/auth/confirm`・`/auth/confirm-recovery`追加、メール確認必須化)と
  Google Cloud Consoleへの新コールバックURL登録(ユーザー作業)が必要

## 06:48 P2-5 仕様書ドラフト作成(アプリ内予定CRUD+Google連携UI凍結)

- やったこと: ユーザー指示「メール認証に伴いアプリ内でも予定を追加できるようにしたい。
  Googleログインは当面凍結予定なので機能除去も検討」を受け、方針をAskUserQuestionで確定
  (①Google連携はUI導線のみ環境フラグで凍結・コードは保持・P0-5保留、②予定は作成+編集+削除、
  ③データモデルは synced_events に source列追加+`app:<uuid>` キーで既存の google_event_id
  紐づけを流用)。プラン承認後、`docs/specs/P2-5_アプリ内予定とGoogle連携凍結.md` をドラフト作成し、
  要件定義書(v0.3: コアループ・FR-02凍結注記・FR-10新設・§8)と開発計画(P2-5行追加・
  P0-5保留・次に着手更新)を改定
- 理由: Google未連携ユーザーには予定レーンが空でコアループ(計画→記録→ギャップ)が成立しない
  ため。ドメイン取得・OAuth審査の長期化を受けGoogle依存を外す方針の継続(2026-07-09の
  P1-3方針転換の延長)
- 結果: 調査で重大リスクを発見 — `app/api/calendar/sync/route.ts` のstale削除は「Google
  レスポンスに無い範囲内の行」を消すため、無対策だとアプリ予定が同期のたびに削除される。
  仕様書にstale削除への `source='google'` フィルタを必須要件として明記(結合テストS16)
- 未解決・次にやること: 仕様書のユーザー承認待ち。承認後にTDD実装
  (マイグレーション→Red→Green→`npm run check`)

## 23:22 P2-5 実装完了(アプリ内予定CRUD+Google連携UI凍結)

- やったこと: 仕様書承認(2026-07-10)を受けてTDDで実装。
  (1) マイグレーション `20260710070000_synced_events_source.sql`(source列+check制約)を
  ローカル適用・検証。(2) シナリオS1〜S20をテストに展開しRed確認(単体8件・結合9件が失敗)。
  (3) 実装: `lib/calendar/app-events.ts`(CRUD+sourceガード)、`event-actions.ts`、
  `components/app-event-panel.tsx`(作成/編集/削除パネル)、`calendar-view.tsx`(追加ボタン・
  ✎編集導線・空状態誘導・googleEnabled分岐)、`lib/google/integration-flag.ts`
  (`GOOGLE_INTEGRATION_ENABLED`、未設定=凍結)、同期APIのstale削除に`source='google'`
  フィルタ+凍結時404、Google connect/callbackも凍結時404、calendar/settingsページのフラグ配線
- 理由: Google未連携でもコアループ(計画→記録→ギャップ)を完結させるため。凍結はUI導線のみで
  コード・テストは保持(仕様書P2-5)
- 結果: 単体テスト186件(+14)・結合テスト56件(+9)全合格、`npm run check`
  (typecheck/lint/test/build)合格、Prettier整形済み。シナリオ20件とテストの対応を突き合わせ確認。
  Playwright 375pxでM1(作成→タイマー→オーバーレイ→サマリー紐づき)・M2(編集反映・削除後に
  実績が割り込みへ再分類)・M3(凍結時Google導線ゼロ/`GOOGLE_INTEGRATION_ENABLED=true`で
  設定画面の連携セクション・更新ボタン・バナーが復活)を確認。確認用ユーザーは削除済み
- 補足: 結合テストが並列実行で不安定だった原因は、summary等の既存テストがadminで
  テーブル全行削除するクリーンアップを持つこと(既存の分離不備)。共有ローカルDBのため
  `vitest.config.integration.ts` に `fileParallelism: false` を設定して直列化(2回連続合格を確認)
- 未解決・次にやること: コミット・PR(ユーザー指示待ち)。本番Supabaseへの `npx supabase db push`
  (source列。未適用のまま本番デプロイするとアプリ予定作成が失敗する)。凍結解除時は
  Vercelに `GOOGLE_INTEGRATION_ENABLED=true` を設定する
