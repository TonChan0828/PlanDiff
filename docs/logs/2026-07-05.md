# 行動履歴 2026-07-05

## 仕様書設計 第1バッチ(P0-6 / P0-3 / P1-1)

- やったこと:
  - `docs/指示資料/` を確認(README のみ、ユーザー資料なし)
  - 開発計画の「次に着手(推奨順)」に従い、仕様書が必要な直近3項目のドラフトを作成:
    - `docs/specs/P0-6_DBスキーマとRLS.md`(コア4テーブル+RLS。マイグレーションSQL全文ドラフト、結合テスト実行基盤 `tests/integration/` の新設方針、シナリオS1〜S10)
    - `docs/specs/P0-3_プライバシーポリシーと利用規約.md`(OAuth審査必須の公開ページ最小版。Limited Use準拠宣言の必須文言、シナリオS1〜S6)
    - `docs/specs/P1-1_Google認証.md`(signInWithOAuthのオプション仕様、callback Route Handlerでのrefresh token保存、再認可導線、シナリオS1〜S10+手動疎通M1/M2)
  - `docs/開発計画.md` の該当3行を「承認待ち」に更新、仕様書パスを記入
- 理由: ユーザー指示(/loop)「実装すべき機能の詳細仕様書を設計してください」。spec-driven-dev / db-migration Skill のワークフローに従い、実装前の仕様書ドラフトまでを実施(承認なし実装禁止のため実装は行わない)
- 主な設計判断(承認時に見てほしい点):
  - time_entries / synced_events の時間制約は `end_at >= start_at`(開始直後の即停止によるゼロ秒実績を許容)
  - time_entries.google_event_id は synced_events へのFKにしない(再同期でキャッシュ行が消えても実績を守る)
  - google_tokens はアクセストークンキャッシュ列を持たない(P1-2で必要になったら追加。先回りしない)
  - 結合テストは `npm run check` に含めず `npm run test:integration`(ローカルSupabase前提、未起動時はfail)として分離
  - P0-3: 連絡先メールアドレス・運営者表記はユーザー確認事項として仕様書に明記
  - P1-1: refresh token保存はcallbackの1点のみ(セッションに永続しないため)。保存失敗時もログインは成立させ再認可導線へ
- 結果: 仕様書3本ドラフト完了、承認待ち。コード変更なし(テスト・ビルドへの影響なし)
- 未解決・次にやること:
  - ユーザーによる3仕様書のレビュー・承認(P0-3の連絡先・運営者表記の確認事項に回答が必要)
  - 次バッチの仕様書設計: P1-2(カレンダー同期)→ P2-1(カレンダービュー)→ P2-2(予定連動タイマー)の順を予定
  - P0-1 / P0-2(Supabase・Google Cloudセットアップ)はユーザー作業のため着手指示待ち

## 11:00–11:45 第1バッチ実装(P0-6 → P0-3 → P1-1)

- やったこと:
  - **承認**: ユーザーが実装計画(プランモード)を承認。AskUserQuestionで確認: ①3仕様書を一括承認 ②P0-3の運営者=個人名+showsphere1028@gmail.com(正確な個人名表記は本文レビュー時に確定、制定日=公開日) ③P1-1まで一気に実装。3仕様書を「承認済み(2026-07-05)」に更新
  - **P0-6(DBスキーマ+RLS)**:
    - 結合テスト基盤新設: `vitest.config.integration.ts`(node環境)+ `tests/integration/helpers.ts`(service roleで一時ユーザー作成→anonサインイン→終了時削除)+ `npm run test:integration`。単体側configから `tests/integration/**` を除外。devDep `postgres` 追加(pg_class・cascade検証用のDB直接接続)
    - ローカルSupabase起動(Docker Desktop未起動だったため `open -a Docker` で起動→ `npx supabase start`)。`.env.local` にローカル値を設定
    - TDD: S1〜S10の結合テスト作成→未適用でRed(10件全失敗を確認)→ `supabase/migrations/20260705022502_core_schema.sql` 作成→ `db reset` →Green(10件全合格)。`npx supabase db diff` で差分なしを確認
    - **仕様書からの逸脱(補記済み)**: SupabaseのDataAPI新デフォルト(auto_expose無効)により新規テーブルがロールに公開されないため、意図するアクセス範囲と1:1の明示GRANTを追加(google_tokensはservice_roleのみ。anon/authenticatedへのGRANTなし)。アクセス制御の実質はRLS側で不変
  - **P0-3(プライバシーポリシー・利用規約)**:
    - TDD: S1〜S4のテスト作成→Red→ `(marketing)/layout.tsx`(共通フッター)+ `/privacy` + `/terms` + `components/legal-article.tsx` 実装→Green。S5はRedフェーズで必須文言検知を確認、S6はビルド出力で `/privacy` `/terms` の静的生成を確認
    - トップページを `app/page.tsx` → `app/(marketing)/page.tsx` に移動(CLAUDE.mdのディレクトリ方針と、S4の「レイアウト×ページ連携」を成立させるため)。create-next-appの英語ボイラープレートを最小限のPlanDiff紹介文に置換、ルートlayoutのmetadata/langを更新(公開ページとして不適切なため)
    - 運営者名・制定日は `[運営者名]` `[制定日]` のプレースホルダー。**公開前にユーザーレビューで確定が必要**
  - **P1-1(Google認証)**:
    - Next16同梱docsを確認: **Middlewareは`proxy.ts`に改名**(機能同一)、`cookies()`はasync、@supabase/ssr 0.12はgetAll/setAll方式 → この規約で実装
    - TDD: 単体6件(S1, S4×2, S5, S9, S10)+結合5件(S2, S3, S6, S7, S8)を作成→Red確認→実装→Green
    - 実装: `lib/supabase/{auth-options,browser,server,admin}.ts`(adminは`server-only`付き・`saveGoogleRefreshToken`はトークン値をログに出さない)、`proxy.ts`(セッション更新)、`components/google-sign-in-button.tsx`、`/login`、`/auth/callback`(Route Handler)、`(app)/layout.tsx`(認証ガード)、`/auth/reauthorize`、`/calendar`(プレースホルダー)、`signOutAction`。dep `server-only` 追加
    - **仕様書からの逸脱(補記済み)**: セッション交換失敗は `/login?error=failed` に分離(ドラフトはキャンセルと同じ `error=auth` で、規定された2種のメッセージを区別できないため)
  - **375px検証**(Playwright、幅375×667): `/` `/privacy` `/terms` `/login` の4ページで横スクロールなし・表示崩れなし、フッターリンク/ボタンの高さ44pxを確認。`/auth/reauthorize` は認証必須のため手動疎通M1/M2時に確認する
- 理由: ユーザー指示「実装計画を立ててください」→計画承認(仕様書3本の一括承認を含む)
- 結果(検証出力の要約):
  - `npm run check`: 全通過(typecheck / lint / test 11件 / build成功。ルート: `/` `/privacy` `/terms` 静的、`/auth/callback` `/auth/reauthorize` `/calendar` `/login` 動的、Proxy認識)
  - `npm run test:integration`: 15件全合格(P0-6: 10、P1-1: 5)
  - シナリオ突き合わせ: P0-6=結合10/10、P0-3=自動4+Red確認(S5)+ビルド確認(S6)、P1-1=単体6+結合5(シナリオ10に対し11テスト)。skip/todo/コメントアウトなし
- 未解決・次にやること:
  - **ユーザー作業**: P0-1(Supabaseプロジェクト作成・`.env.local`更新・`supabase link`)、P0-2(Google Cloud OAuthクライアント+同意画面。スコープはcalendar.events.readonlyのみ)
  - P1-1の手動疎通M1/M2(P0-1/P0-2完了後): M1=実Googleアカウントで初回ログイン→Studioで`google_tokens`にrefresh token保存を確認 / M2=連携済みアカウントで再認可導線の動作確認。`/auth/reauthorize`の375px確認もこのとき実施
  - P0-3の条文レビュー(運営者名・制定日の確定)→コミット
  - コミットは未実施(ユーザー確認後にステップ単位で実施予定)
  - 次: P0-4(Vercelデプロイ)→P0-5(OAuth審査申請)、仕様書設計はP1-2から

## 12:00頃 コミット(ステップ単位)と条文レビュー確定

- やったこと:
  - ユーザー確認: ステップ単位で3コミットする方針と、P0-3条文の事前レビューを選択
  - コミット1 `0287e1b`(P0-6: マイグレーション+結合テスト基盤)、コミット2 `0a33878`(P1-1: Google認証一式)。いずれもpre-commitフック(format:check/typecheck/lint/test/build)通過
  - 条文全文を提示しレビュー完了。**運営者名=「Sho Takasaki」で確定**(privacy/terms両ページに反映)。**制定日はP0-4デプロイ時に確定**(`[制定日]` プレースホルダー残置、コード内コメントで明示)
  - 仕様書P0-3の確認事項・開発計画(P0-3を完了に)を更新し、P0-3+ドキュメント一式をコミット
- 理由: ユーザー指示(コミット方針の選択+条文レビュー回答)
- 結果: 開発計画の状態 = P0-6完了 / P0-3完了(公開はP0-4)/ P1-1実装中(手動疎通待ち)
- 未解決・次にやること: P0-1/P0-2(ユーザー作業)→ P0-4デプロイ(制定日差し替え)→ P0-5審査申請。P1-1のM1/M2手動疎通

## 13:00頃 P0-1/P0-2セットアップ併走 + P1-1手動疎通(M1/M2)完了

- やったこと:
  - **P0-2(ユーザー作業を案内)**: Google Cloudプロジェクト・Calendar API有効化・OAuth同意画面(外部/テスト中/テストユーザー登録)・OAuthクライアント作成(リダイレクトURI: ローカル `http://127.0.0.1:54321/auth/v1/callback` とリモート `https://<ref>.supabase.co/auth/v1/callback` の両方)
  - **P0-1**: Supabaseプロジェクト作成(ユーザー作業。ref: bkcppesfpayjtftlfmoz / 東京)→ CLIログイン済みを確認 → `supabase link` → `db push` は権限分類器により自動実行不可のため**ユーザーが実行**(成功。`migration list` でローカル=リモート一致を確認。pg-deltaカタログキャッシュの警告は適用に影響なし)
  - **ローカルGoogleプロバイダ設定**: `supabase/config.toml` に `[auth.external.google]`(`env(GOOGLE_CLIENT_ID)`/`env(GOOGLE_CLIENT_SECRET)` 参照、`skip_nonce_check = true`)と `additional_redirect_urls`(localhost/127.0.0.1の`:3000`と`/auth/callback`)を追加。シークレットは `.env.local`(gitignore済み)にユーザーが記入。`.env.local` をsourceして `supabase stop/start` で反映し、`/auth/v1/settings` で google=true を確認
  - **M1(初回ログイン疎通)**: 実Googleアカウントで `/login` → 同意 → `/calendar` 到達(表示名の表示も確認)。service role経由で `google_tokens` に1行保存を確認(**トークン値は表示せず** user_id/created_atのみ)。profilesトリガーによる自動作成も確認
  - **`/auth/reauthorize` の375px検証**: ローカル専用の一時パスワードユーザーでセッションcookie(@supabase/ssr形式)を生成しPlaywrightに注入して確認。横スクロールなし・ボタン44px。一時ユーザーは検証後に削除
  - **M2(再認可導線疎通)**: service roleで `google_tokens` の行を削除し未連携状態を再現 → ユーザーが `/auth/reauthorize` から再認可 → 行の再作成を確認(created_atが新しい値に更新)
- 理由: ユーザー指示「ガイドして」(P0-1/P0-2セットアップの併走)+ P1-1のDoD(手動疎通M1/M2)の消化
- 結果: **P0-1完了 / P0-2完了 / P1-1完了(DoD全達成)**。開発計画を更新
- 未解決・次にやること:
  - P0-4(Vercelデプロイ): リモートSupabaseのURL/キーとGoogle認証情報をVercel環境変数に設定、`[制定日]` の確定、本番URLをGoogle(リダイレクトURI)とSupabase(URL Configuration)に追加
  - P0-5(OAuth審査申請)
  - P1-2(カレンダー同期)の仕様書設計
  - config.toml変更+ドキュメント更新のコミット(ユーザー確認待ち)
