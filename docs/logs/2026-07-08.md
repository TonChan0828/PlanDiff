# 2026-07-08

## P2-4: 実績の手動編集(仕様書作成→承認→実装→検証)

- やったこと:
  - `docs/開発計画.md` の「次に着手」からP2-4(実績の手動編集、FR-08)を選択。`docs/指示資料/` を確認(README以外に関連資料なし)
  - 仕様書 `docs/specs/P2-4_実績の手動編集.md` を作成しユーザー承認を取得(2026-07-08)
  - テストシナリオS1〜S21をRed→Green:
    - `lib/timer/blocks.ts`: `actualBlockInputs` に `editable` フラグを追加(確定済み=true、実行中=false)
    - `lib/timer/service.ts`: `updateTimeEntry` / `deleteTimeEntry` を追加。`end_at IS NOT NULL` をガード条件に含め実行中エントリを直接操作できないようにした(サーバー側の二重防御)
    - `lib/timer/messages.ts`: 編集パネル用文言を追加
    - `components/edit-entry-panel.tsx` を新規作成: タイトル・開始/終了(`datetime-local`)・削除(2段階確認、ネイティブ`confirm()`は使わない)のモーダル編集パネル
    - `app/(app)/calendar/timer-actions.ts`: `updateTimeEntryAction` / `deleteTimeEntryAction` を追加
    - `components/calendar-view.tsx`: 実績ブロック(`ActualBlock`)をeditableなら`button`化しタップで編集パネルを開くよう変更。保存/削除はServer Action成功後に`router.refresh()`(楽観的更新はしない方針を仕様書に明記)
  - スキーマ変更・マイグレーションはなし(P0-6で作成済みのupdate/delete RLSポリシーをそのまま使用)
  - 375px手動疎通(ui-quality Skill): 一時的な `/api/dev-login` ルートを作成しローカルSupabaseの検証ユーザー+確定済み実績2件(通常タイトル・タイトルなし)を投入。Playwrightで `/api/calendar/sync` をモックして実表示。実績ブロックタップ→編集パネル→タイトル/開始/終了を変更して保存→DB反映(`check-db`スクリプトで確認)→ブロック表示更新、を確認。別エントリで削除→確認表示→「削除する」→ブロック消滅も確認。ダークモード・タップターゲット44px(保存/削除/閉じる)も確認。検証ユーザー・一時ルート・スクリーンショット・確認スクリプトはすべて削除済み
- 理由: 開発計画 P2-4(FR-08)。記録漏れ・記録ミスを後から直せることがギャップ分析(P3-2)の前提になるため
- 結果: `npm run check` 合格(typecheck + lint + test 98件 + build)。結合テスト40件全合格(P2-4分は新規6件: S16〜S21)。仕様書の全シナリオS1〜S21にテストが対応
- 未解決・次にやること:
  - M1(実環境: 確定済み実績をタップ→編集→保存→表示反映)/ M2(実績をタップ→削除→確認表示→削除→消える。実行中ブロックは反応しない)はユーザーの手動確認待ち(P2-1〜P2-3と同じ運用)
  - 確認後に開発計画のP2-4を「完了」へ。コミット→PRはユーザー指示で

## P2-4: 手動疎通M1/M2合格 → 完了

- やったこと: ユーザーからM1(確定済み実績をタップ→編集→保存→表示反映)/ M2(実績をタップ→削除→確認表示→削除→消える)の合格報告を受領。開発計画のP2-4を「完了」に更新し、「次に着手」をP3-1(オーバーレイ表示)に更新(Phase 2完了のため)
- 結果: P2-4完了。Phase 2(P2-1〜P2-4)がすべて完了
- 未解決・次にやること: コミット・PR作成はユーザー指示で。次の実装候補はP3-1(オーバーレイ表示、FR-06)またはP0-5(OAuth審査申請)

## PR作成(P2-4)

- やったこと: ユーザー指示「コミット PR作成」。`feat/p2-4-manual-edit` ブランチ(コミット1件、mainからの差分13ファイル)をpush、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/7(Vercel Previewデプロイが走る想定)
- 未解決・次にやること: レビュー・マージ待ち。マージ後はローカルmainをorigin/mainに追従。次の実装候補はP3-1(オーバーレイ表示)またはP0-5(OAuth審査申請)

## PR #7 CI失敗の調査・修正

- やったこと: ユーザー報告「CIが失敗する」を受けて調査。`gh run list` でP1-2以降のCI実行(main push・各PR)がすべて同じ理由で失敗し続けていたことを確認(P2-4固有の問題ではない)。原因は `lib/supabase/server.ts` の `createClient()` が `cookies()` を呼ぶ前に環境変数未設定を例外で弾く実装になっており、GitHub Actions(Secrets未設定)ではNext.jsの`cookies()`ベースの動的判定より先に例外が発生し、ビルド時の静的プリレンダリングが失敗していたこと。`app/(app)/layout.tsx`(認証必須グループ全体)と`app/(marketing)/login/page.tsx`が該当。ユーザーに修正方針を確認(force-dynamic明示 / CI環境変数追加 / 報告のみ)し、「force-dynamic明示」を選択
- 対応: 両ファイルに `export const dynamic = "force-dynamic"` を追加。`.env.local`を退避してSupabase環境変数なしでの `npm run build` 成功を再現確認してから、同じPR #7ブランチに修正コミットをpush
- 結果: PR #7のCI(check / Vercel / Vercel Preview Comments)がすべてpassに変わったことを確認
- 未解決・次にやること: レビュー・マージ待ち。マージ後はローカルmainを追従

## PR #7マージ → P2-4完了・CI修正確認

- やったこと: ユーザーからPR #7マージ完了の報告を受領。ローカルmainをorigin/main(マージ済み、fast-forward)に追従。マージ済みのfeat/p2-4-manual-editブランチをローカルから削除(リモートはGitHub側で自動削除済み)。mainへのマージpushで走ったCIが`success`になったことを確認(P1-2以降で初めてmainのCIが成功)
- 結果: P2-4完了・マージ済み。CI(force-dynamic)修正がmainでも有効であることを実証
- 未解決・次にやること: 次の実装候補はP3-1(オーバーレイ表示、FR-06)またはP0-5(OAuth審査申請)。ユーザー指示待ち

## P3-1: オーバーレイ表示(仕様書作成→承認→実装→検証)

- やったこと:
  - `docs/開発計画.md` の「次に着手」からP3-1(オーバーレイ表示、FR-06)をユーザーに確認の上選択。`docs/指示資料/` を確認(README以外に関連資料なし)
  - 現状把握: 予定=左レーン(55%)薄い塗り・実績=右レーン(45%)濃い塗りの分離はP2-1/P2-2で実装済みと判明。FR-06の残課題は「紐づく実績/フリー実績の色分け」と「開始遅延・超過の視覚化」の2点のみ
  - 仕様書 `docs/specs/P3-1_オーバーレイ表示.md` を作成。判断が必要な2点(①同じ予定に紐づく実績が複数ある場合の開始遅延/超過の割り当て方=最初/最後の実績のみに表示、②早期開始・早期終了の可視化はスコープ外)をユーザーに確認の上、両方ドラフト通り承認を取得(2026-07-08)
  - テストシナリオS1〜S13をRed→Green:
    - `lib/timer/gap.ts`(新規): `computeActualGaps` — 予定と実績の紐づき判定・開始遅延/超過時間を計算する純粋関数。中断・再開(同じ予定に実績が複数)は時系列で最初のみ開始遅延、最後のみ超過を計算
    - `lib/timer/types.ts` / `lib/timer/entries.ts` / `lib/timer/blocks.ts`: `TimeEntryItem`・`fetchTimeEntries`・`actualBlockInputs` に `googleEventId` を追加(`fetchRunningEntry`と同じパターン)
    - `lib/timer/messages.ts`: `delayLabel` / `overrunLabel` / `freeBadge` を追加
    - `components/calendar-view.tsx`: `DayColumn`でその日の予定・実績から`computeActualGaps`を呼び、`ActualBlock`に紐づき状態(sky=紐づく/amber=フリー・割り込み)とズレ情報を渡す。開始遅延は予定開始〜実績開始の区間、超過は予定終了〜実績終了の区間に、ストライプ柄の装飾バー(`GapStripe`、色以外の手がかりとして柄を使用)を表示。日ビューでは「開始N分遅れ」「N分超過」「フリー」のテキストラベルも表示(週ビューは省略、`aria-label`には常に含める)
    - 既存テスト(P2-4分)のfixtureに`googleEventId`を追加。「朝会」(googleEventIdなし=フリー扱い)のaria-label期待値を実際の挙動(`(フリー)`サフィックス付与)に合わせて修正
  - スキーマ変更・マイグレーションはなし(既存の`time_entries.google_event_id`列を読むだけ)
  - 375px手動疎通(ui-quality Skill): 一時的な`/api/dev-login`ルートを作成する際、自動モードのセキュリティチェックで「未認証+service roleキーでユーザー作成する裏口」としてブロックされたため、ユーザーに確認の上`NODE_ENV !== "development"`で404を返す本番ガードを追加してから実行。ローカルSupabaseの検証ユーザー+予定4件・実績5件(開始遅延のみ/超過のみ/遅延+超過/ズレなし/フリー)を投入し、Playwright(独立ブラウザコンテキスト、ユーザーの実Chromeとは別)で`/api/calendar/sync`をモックして実表示を確認。日ビュー(遅延・超過・フリーそれぞれのストライプ・ラベル)、週ビュー(ラベル省略・色柄は維持)、ダークモードを確認。検証ユーザー・一時ルート・スクリーンショット・クリーンアップスクリプトはすべて削除済み(git statusで残存ファイルなしを確認)
- 理由: 開発計画P3-1(FR-06)。「プロダクトの顔」としてUI品質に最も投資する項目のため、色分け・ズレの視覚化を色以外の手がかり(柄・テキスト)も併用して実装した(ui-quality Skillの色覚多様性配慮)
- 結果: `npm run check`合格(typecheck + lint + test 110件 + build)。結合テスト41件全合格(P3-1分は新規1件: S13)。仕様書の全シナリオS1〜S13にテストが対応
- 未解決・次にやること:
  - 手動疎通は本セッションのPlaywrightで確認済みだが、ユーザー自身による実環境(ブラウザ)での最終確認はまだ。確認後に開発計画のP3-1を「完了」へ
  - コミット・PR作成はユーザー指示で

## PR作成(P3-1)

- やったこと: ユーザー指示「コミット PR作成」。`feat/p3-1-overlay-display`ブランチ(コミット1件、mainからの差分15ファイル)をpush、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/8(Vercel Previewデプロイが走る想定)
- 未解決・次にやること: レビュー・マージ待ち。マージ後はローカルmainをorigin/mainに追従

## PR #8マージ → P3-1完了

- やったこと: ユーザーからPR #8マージ完了の報告を受領。ローカルmainをorigin/main(マージ済み、fast-forward)に追従。マージ済みのfeat/p3-1-overlay-displayブランチをローカルから削除(リモートはGitHub側で自動削除済み)。mainへのマージpushで走ったCIが`success`になったことを確認。開発計画のP3-1を「完了」に更新し、「次に着手」をP3-2(ギャップサマリー)に更新(P3-1完了のため)
- 結果: P3-1完了・マージ済み
- 未解決・次にやること: 次の実装候補はP0-5(OAuth審査申請)またはP3-2(ギャップサマリー)。ユーザー指示待ち

## P3-2: ギャップサマリー(仕様書作成→承認→実装→検証)

- やったこと:
  - `docs/開発計画.md` の「次に着手」からP3-2(ギャップサマリー、FR-07)をユーザーに確認の上選択。`docs/指示資料/` を確認(README以外に関連資料なし)
  - 仕様書 `docs/specs/P3-2_ギャップサマリー.md` を作成。判断が必要な2点(①範囲をまたぐ予定・実績は開始時刻基準でのみ判定し按分計算はしない、②カレンダー画面ヘッダーにサマリーへの導線を追加する)をユーザーに確認の上、両方ドラフト通り承認を取得(2026-07-08)
  - テストシナリオS1〜S15をRed→Green:
    - `lib/summary/aggregate.ts`(新規): `computeGapSummary` — 予定・実績とも開始時刻が対象範囲内のものだけを集計し、予定単位の計画/実績時間・ズレ・未着手判定、紐づかない実績(割り込み・フリー)の一覧、期間合計のズレ%(計画0分のときはnull)を計算する純粋関数
    - `lib/summary/format.ts`(新規): 分数を「3時間15分」形式、ズレを符号付き(「+20分」等)で表示するフォーマッタ
    - `lib/summary/messages.ts`(新規): サマリー画面の日本語文言
    - `app/(app)/summary/page.tsx`(新規): 今日/今週タブ(`?range=today`|`?range=week`、Server Componentのまま`<Link>`遷移)、集計カード、予定単位の内訳、割り込み・フリー作業一覧を表示。既存の`fetchSyncedEvents`/`fetchTimeEntries`/`fetchRunningEntry`(基準日±1週間取得)をそのまま利用し追加のDB呼び出しなし。実行中タイマーは既存の`actualBlockInputs`でマージし実績合計に反映
    - `app/(app)/calendar/page.tsx`: ヘッダーに「サマリー」への導線を追加(サマリー画面には「カレンダーへ戻る」を追加)
  - 結合テストは、既存のP2-1の方針(page自体はレンダリングせず、データ取得〜集計のロジックをローカルSupabase+RLSで検証)に倣い`tests/integration/summary.test.ts`として実装
  - スキーマ変更・マイグレーションはなし(既存の`synced_events`/`time_entries`を読むだけ)
  - 375px手動疎通(ui-quality Skill): P3-1と同じ手法(本番ガード付き一時`/api/dev-login`ルート+ローカルSupabase+独立Playwrightブラウザ)で実施。ポート3000/3001が別プロセスで使用中だったため`PORT=3002`で起動。予定3件(未着手/ズレなし/超過)+割り込み1件を投入し、今日/今週タブ・ダークモード・カレンダー画面からの導線を確認。**手動疎通中にカレンダー画面ヘッダーの375px崩れを発見**(見出し「カレンダー」が「サマリー」「ログアウト」ボタンに押されて2行に折り返す)。ヘッダーを`flex-wrap`化しボタン余白を`px-6→px-4`に調整して修正、再確認して解消を確認。検証ユーザー・一時ルート・スクリーンショット・クリーンアップスクリプトはすべて削除済み(git statusで残存ファイルなしを確認)
  - `npm run test:integration` 実行時に`tests/integration/core-schema.test.ts`が並列実行下で1件失敗する事象を確認したが、P3-2の変更を`git stash`で退避した状態でも同じ失敗が再現したため、既存の(並列実行時のみ発生する)flakyテストでありP3-2起因ではないと判断。単体実行では合格することを確認済み。対応は別途の課題として記録のみ
- 理由: 開発計画P3-2(FR-07)。オーバーレイ表示(視覚的なズレ把握)に対し、本機能は「計画◯時間 vs 実績◯時間、ズレ+◯%」の定量把握を担う(要件定義書 §1のコアバリュー)
- 結果: `npm run check`合格(typecheck + lint + test 119件 + build)。結合テスト(`npm run test:integration`単独実行)全合格。仕様書の全シナリオS1〜S15にテストが対応
- 未解決・次にやること:
  - `tests/integration/core-schema.test.ts`の並列実行時flakyテスト(P3-2起因ではない、既存の課題)。対応方針は別途検討
  - ユーザー自身による実環境での最終確認はまだ。確認後に開発計画のP3-2を「完了」へ
  - コミット・PR作成はユーザー指示で

## P3-2: 実環境確認合格 → 完了・PR作成

- やったこと: ユーザーから実環境での最終確認の合格報告を受領。開発計画のP3-2を「完了」に更新し、「次に着手」をP3-3(PWA対応)に更新(Phase 3の残りはP3-3のみのため)。`feat/p3-2-gap-summary`ブランチで変更をコミット(pre-commitフック全項目合格)・push、PR作成
- 結果: P3-2完了。PR: https://github.com/TonChan0828/PlanDiff/pull/9
- 未解決・次にやること: レビュー・マージ待ち。マージ後はローカルmainをorigin/mainに追従。次の実装候補はP0-5(OAuth審査申請)またはP3-3(PWA対応)

## PR #9マージ確認・CI結果確認

- やったこと: ユーザー指示「CIの結果を確認して、開発計画・ログを更新して」を受け、`gh pr view 9` / `gh run list` で状態を確認。PR #9は`mergedAt: 2026-07-08T12:42:20Z`でmainにマージ済み(ローカルmainは`git status`で`origin/main`とfast-forward済み・差分なしを確認)。直近のCI実行(mainへのpush含む)はすべて`success`
- 結果: PR #9マージ・CI成功を確認。開発計画のP3-2行は完了のまま変更なし。「次に着手」を1件目P3-3・2件目P0-5の順に更新(P3-3の仕様書ドラフトが既にあるため)
- 未解決・次にやること: なし(この確認作業自体は完了)

## P3-3: PWA対応 仕様書ドラフト作成

- やったこと:
  - `docs/開発計画.md`の「次に着手」からP3-3(PWA対応、FR-09)とP0-5(OAuth審査申請)をユーザーに提示し、P3-3を選択。`docs/指示資料/`を確認(README以外に関連資料なし)
  - Next.js 16のファイル規約(`node_modules/next/dist/docs`内のmanifest.md/app-icons.md/generate-viewport.md)を確認した上で仕様書`docs/specs/P3-3_PWA対応.md`を作成:
    - `app/manifest.ts`(Web App Manifest)、`app/icon-192・icon-512`(Route Handlerでの`ImageResponse`生成)、`app/apple-icon.tsx`、`app/layout.tsx`への`viewport`(themeColorのlight/dark)追加という構成
    - デザイン素材が未用意のため、アイコンはブランドカラー(sky-600、オーバーレイ表示P3-1と同じ色)+白文字「P」の簡易生成とした
    - Service Worker/オフラインキャッシュ・プッシュ通知はスコープ外と明記(要件定義書に明記なし、v2扱いの判断)
  - 開発計画のP3-3行を「承認待ち」に更新、仕様書列にパスを記入
- 理由: 開発計画の次項目としてP3-3(FR-09)をユーザーが選択したため
- 結果: 仕様書ドラフトを提示済み。ユーザーの承認待ち(まだ実装コードは書いていない)
- 未解決・次にやること: ユーザー承認後、テストシナリオS1〜S7のRed→Green、手動疎通M1/M2の実施
