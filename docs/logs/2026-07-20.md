## 開始 P5-5 時刻ステッパー入力

- やったこと: ユーザー要望「分を00→59に減らすとき時刻も-1する」を受け、
  `docs/指示資料/` を確認(関連資料なし)。既存の時刻編集UI(3パネル、すべて
  `<input type="datetime-local">`)を調査し、ネイティブ入力は分の折り返しが
  時に波及しないことを確認。ユーザーとAskUserQuestionで方式(カスタムステッパーに
  置換)と適用範囲(予定編集含む3パネル全部)を確認。plan modeでプラン作成・承認を得た。
  `docs/specs/P5-5_時刻ステッパー入力.md` を作成し、`docs/開発計画.md` に
  P5-5行(状態=承認待ち)を追加。作業ブランチ `feature/p5-5-datetime-stepper` を作成
- 理由: CLAUDE.mdの開発方針(仕様書なし・承認なしの実装禁止、mainへの直接コミット禁止)に従う
- 結果: 仕様書ドラフト作成完了。ユーザー承認待ち
- 未解決・次にやること: 仕様書の承認を得てからテストシナリオ→TDDで実装に着手

## 17:20 P5-5 実装完了(TDD〜検証)

- やったこと: 仕様書をユーザーに提示しAskUserQuestionで承認を得た(ステータスを
  「承認済み」へ更新)。仕様書のテストシナリオS1〜S26に沿ってTDDで実装:
  1. `lib/ui/local-date-time.ts`(純関数。`parseLocalDateTime`/`stepLocalMinute`/
     `stepLocalHour`/`setLocalHour`/`setLocalMinute`/`setLocalDate`。date-fnsの
     `addMinutes`/`addHours`で桁跨ぎを計算)+ `tests/lib/ui/local-date-time.test.ts`(24件)
  2. `components/date-time-stepper.tsx`(`DateTimeStepper`。日付=native
     `type="date"`、時・分=`role="spinbutton"`+▲▼ボタン+直接入力。React Compiler
     のlint(`react-hooks/refs`・`react-hooks/set-state-in-effect`)に抵触したため、
     「レンダー中に前回propsとの差分を見てstateを調整する」公式パターンへ書き換え)+
     `lib/ui/messages.ts` + `tests/helpers/date-time-stepper.ts`(既存テスト共通化)+
     `tests/components/date-time-stepper.test.tsx`(11件)
  3. 3パネル(`edit-start-panel.tsx`/`edit-entry-panel.tsx`/`app-event-panel.tsx`)の
     `datetime-local`を`DateTimeStepper`に置換。既存テスト6ファイルを
     `changeDateTimeStepper`/`expectDateTimeStepperValue`ヘルパーで改修し、
     各ファイルに桁跨ぎ確認シナリオ(P5-5 S15/S24/S26)を追加
  4. 影響する既存仕様書4本(P2-4, D-4, P2-5, P5-1)のdatetime-local記述を
     `DateTimeStepper`/`lib/ui/local-date-time`参照へ更新
  5. 手動疎通: 本番ガード付き一時`/api/dev-login`ルート+ローカルSupabase検証
     ユーザーで実施。claude-in-chromeのwindow resizeがこの環境で効かなかったため
     Playwright MCP(`browser_resize`)に切り替え、375×667で確認:
     - `/track`: フリータイマー開始→開始時刻変更パネルで分00→分1戻す→時が
       17→16・分59に連動(本要望の中心シナリオ)。59分→分1進めるで17:00に復帰
       (繰り上がりも確認)
     - `/calendar`: 実績ブロックタップ→実績編集パネルで同様に00分→59分+時-1を確認
     - `/calendar`「予定を追加」→予定編集パネルで同様に18:00→17:59を確認
     - ダークモード(`document.documentElement.dataset.theme="dark"`)でも表示・
       桁跨ぎ動作を確認。表示崩れ・コントラスト問題なし
- 理由: CLAUDE.mdの開発方針(仕様書承認→シナリオ→TDD)・ui-quality Skill
  (375px確認)に従う。React Compilerのlintは既存プロジェクトのESM設定に
  含まれており、警告を握りつぶさず設計をパターンに合わせて修正した
- 結果: `npm run check`(typecheck+lint+test+build)合格を出力で確認。
  テスト409件全合格(既存371件+新規38件)。手動疎通(Playwright 375px、
  ライト/ダーク)で3パネルとも桁跨ぎ動作を確認。検証用ユーザー・一時ルート
  (`app/api/dev-login`)・スクリーンショット・クリーンアップスクリプトは
  すべて削除済み(`git status`で残存ファイルなしを確認)
- 未解決・次にやること: PR作成しレビュー・マージへ。マージ後に開発計画のP5-5を
  「完了」へ更新。DB変更なしのため本番`db push`は不要
