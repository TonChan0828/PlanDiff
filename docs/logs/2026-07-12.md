# 2026-07-12

## 実メールアドレスの履歴からの完全削除

- やったこと: 2026-07-11に`docs/logs/2026-07-11.md`へ誤って記載してしまった実メール
  アドレスについて、通常のコミットによる伏字化(PR #17)だけでなく「完全に消して」との
  指示を受け、Git履歴自体からの削除に対応した。対象範囲(ログ本文の記載のみ。git
  コミット作成者情報は対象外)をユーザーに確認のうえ、`git-filter-repo`をインストールし、
  スクラッチパッドにリポジトリのミラークローンを作成して作業(作業ディレクトリ本体には
  触れない)。`--replace-text`で全88コミットを対象に実メールアドレス文字列を
  `[実メールアドレス]`へ置換し、`main` / `feature/p4-1-onboarding` /
  `fix/redact-email-in-log` の3ブランチ(リモートに存在した全ブランチ)を
  `force-with-lease`で書き換え後の履歴にpush。新規クローンで全52コミットを再検索し
  実メールアドレスが0件であることを確認。ローカル作業ディレクトリの該当3ブランチも
  新履歴に`reset --hard`で追従させ、既にリモートから削除済みで書き換え対象外だった
  古いローカルブランチ4本(feat/p2-5-app-events-google-freeze 等。旧履歴に実メールを
  含んでいたがどこにもpushされていなかった)はユーザー確認のうえ削除。
  `git reflog expire`+`git gc --prune=now`でローカルの不要オブジェクトも整理し、
  作業用ミラークローンも検証後に削除
- 理由: パブリックリポジトリに個人の実メールアドレスを記載してしまったことへの是正。
  伏字化コミットの追加だけでは過去のコミット差分から復元可能なままだったため、
  ユーザーの明示的な指示と対象範囲の確認を得たうえで、より確実な除去手段である
  履歴書き換えを実施した。GitHub上の既存PRリンク・レビュー履歴への影響(コミットSHAが
  全て変わる)を事前にユーザーへ説明し、了承を得てから実行した
- 結果: リモート・ローカルともに実メールアドレスの記載は0件。ただしGitHub側のキャッシュ・
  検索インデックスに一定期間残る可能性がある点、他に本リポジトリをクローンしている環境が
  あれば旧履歴を保持したままになる点は限界事項としてユーザーへ伝達済み
- 未解決・次にやること: PR #17(伏字化コミットの通常マージ)のレビュー・マージは
  引き続き必要。今後ログに個人情報を記載しない運用を徹底する

## デザイン再検討「時間のdiff」— モック作成と仕様書ドラフト

- やったこと: 「現状の機能ベースでデザインの再検討」の指示を受け、全画面のコードを
  調査して現状診断(実質Arial描画のフォントバグ、色の直書き、画面ごとに不一致な
  ピルナビ、タイムラインの65dvh制限など)をまとめ、「時間のdiff」コンセプト
  (群青×柿トークン、等幅数値のズレ表記、斜線ハッチの紋章化、方眼背景、下部タブの
  アプリシェル)を提案。ユーザーの「モックを見てから判断」の意向を受けて、
  カレンダー日ビュー(375px・ライト/ダーク)のHTMLモックをArtifactとして作成
  (https://claude.ai/code/artifact/64ba1855-fcee-441f-93f1-f79dfc468eef)。
  方向性の承認(スコープ: D-1+D-2)を得て、仕様書
  `docs/specs/D-1-2_デザイン刷新.md` をドラフト作成し、開発計画にD-1〜D-3を追加
- 理由: 機能はPhase 0〜4前半まで揃ったが、見た目が素のTailwind既定値で
  プロダクトの「顔」(オーバーレイ)に投資できていなかったため。CLAUDE.mdの
  「UI品質はオーバーレイに最優先で投資」に沿う
- 結果: モックはユーザー確認済み(「OK」)。仕様書はドラフト(承認待ち)。
  実装・コード変更は未着手
- 未解決・次にやること: 仕様書の承認を得る → 承認後にfeatureブランチでTDD実装
  (S1〜S9)→ M1(Playwright 375px)→ M2(ユーザー確認)

## D-1+D-2 デザイン刷新の実装(TDD)

- やったこと: 仕様書承認を受け `feature/d-1-2-design-refresh` ブランチでTDD実装。
  Red: 新規テスト5ファイル(BottomTabBar S1〜S3 / AppBar S6 / ズレ表記 S4 /
  カレンダーページ S5 / (app)レイアウト S8)+既存3ファイルの期待値更新
  (オーバーレイ文言・manifest/viewportの色)を先に書き、失敗を確認。
  Green: (1)globals.cssにトークン体系(方眼紙/宵・墨・群青・柿・紅+方眼/ハッチ用
  CSS変数)を@themeで一元化、Arialがgeistを上書きするフォントバグを修正、
  `color-scheme: light dark` 追加。(2)AppBar(Plan=アウトライン/Diff=塗りの
  ワードマーク)とBottomTabBar(4タブ・aria-current・safe-area対応)を新設し
  (app)レイアウトをh-dvhシェル化。calendar/track/summary/settingsの
  ピルナビ・戻るリンク・「さんとしてログイン中」を削除。(3)カレンダーの
  65dvh制限を撤廃して残り高さへ拡大、方眼背景・現在時刻チップ・ズレ表記の
  符号付き化(+N分 遅れ/超過)・群青×柿の塗り分け・タイマーバー刷新。
  panels/バナー/オンボーディング等もトークンへ追随(zinc/sky/amber/red直書きを
  全廃)。lib/pwa/theme.tsのブランド色を#2f4acb・ダーク背景を#0e1116に変更
- 理由: 仕様書 docs/specs/D-1-2_デザイン刷新.md(承認済み2026-07-12)。
  なお仕様書S8は結合テストの実現方法をレイアウト+シェルのコンポーネント間
  連携検証(Supabaseクライアントはモック)へ実装前に文言修正した
  (既存結合テストの方式ではServer Componentの描画を伴わないため)
- 結果: 単体・コンポーネント236件(+12件)/結合64件 全合格。`npm run check`
  合格(typecheck+lint+test+build)。M1: ローカルSupabaseにシードユーザーを
  作成しPlaywright 375×667でcalendar(日/週)・track・summary・settingsの
  ライト/ダーク計10枚を確認。途中で発見した2件
  (ダークのスクロールバーが白い→color-schemeで修正、サマリー合計値の
  折返し→whitespace-nowrap+サイズ調整)も修正済み
- 未解決・次にやること: M2(ユーザーの実環境確認)。確認後に開発計画の
  状態を「完了」へ更新する。D-3(サマリーdiffヒーロー+LP)は未着手のまま
