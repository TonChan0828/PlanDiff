# 2026-07-18

## アーキテクチャ解説資料の新規作成

- やったこと:
  - `docs/アーキテクチャ解説.md` を新規作成。初参加の開発者向けに、プロダクト概要(コアループ・
    Google連携凍結の経緯)、技術スタック、全体アーキテクチャ(3種のSupabaseクライアントの
    使い分け)、ルーティング構成、データモデル(ER図)、主要データフロー(タイマー/Google同期/
    定期予定実体化/予定提案のシーケンス図)、オーバーレイUIの構造、初学者のハマりどころ、
    コードリーディングの入口4ファイル、開発ワークフロー・テスト構成を1つにまとめた
  - 図はすべてGitHubでレンダリング可能なMermaid(flowchart / erDiagram / sequenceDiagram)で記載
  - 内容はコードベース調査(app/ components/ lib/ supabase/migrations/ tests/)と
    `docs/要件定義書.md`・`docs/specs/` 全22仕様書・直近ログの突合に基づく
- 理由: ユーザーからの依頼「初めて開発する人にもわかるようにアーキテクチャ・仕組み・仕様を
  図付きでまとめてほしい」。情報が要件定義書・仕様書・CLAUDE.md・コードに分散しており、
  新規参加者が全体像を掴むコストが高かったため
- 結果: ドキュメントのみの追加でコード変更なし(`npm run check` の対象変更なし)。
  「Google連携は凍結中でメール認証+アプリ内予定が主導線」「synced_events に3種の予定が
  source と google_event_id プレフィックス(app:/rec:)で同居」という誤解しやすい2点を
  資料の冒頭・データモデル節で明示した
- 未解決・次にやること: なし

## PR #31 マージ

- やったこと: 上記資料を `docs/architecture-guide` ブランチ(origin/main起点)でコミットし
  PR #31 を作成、ユーザーがレビュー・マージ。pre-commitフック全項目通過
  (format:check / typecheck / lint / テスト346件 / build)
- 理由: mainへの直接コミット禁止のため、ドキュメントのみでもブランチ+PRで反映する運用
- 結果: マージ完了。ローカルmainをfast-forwardし作業ブランチを削除。
  本記録自体も同運用に従い別PRで反映

## P5-3 サマリー件数ステータス行(仕様策定〜実装)

- やったこと:
  - ユーザー要望「サマリーページで参照できる項目を追加したい」から検討を開始。
    brainstormingで方向性を確認し、①開始遅延 ②予定単位ズレ% ③件数系 ④見積もり係数(v2)
    ⑤タイトル横断 ⑥期間拡張 の6候補から「③件数系の集計」「ヒーロー直下のステータス行」で確定
  - `docs/specs/P5-3_サマリー件数ステータス行.md` を作成しユーザー承認(2026-07-18)。
    開発計画にP5-3行を追加
  - TDD(S1〜S8のRed確認→Green):
    - `lib/summary/aggregate.ts`: `GapSummary` に `planCount` / `startedCount` /
      `notStartedCount` / `interruptionCount` / `interruptionTotalMinutes` を追加
    - `lib/summary/format.ts`: `formatSummaryCounts`(予定0件で着手・未着手省略、
      割り込み0件で「割り込みなし」)。文言は `lib/summary/messages.ts` に集約
    - `app/(app)/summary/page.tsx`: ヒーローのメタ行直下に `gap-hero-counts` を描画
    - テスト追加: `tests/lib/summary/aggregate-counts.test.ts`(S1・S7)、
      `format-counts.test.ts`(S2・S4〜S6)、`tests/app/(app)/summary/page-counts.test.tsx`(S3)、
      `tests/integration/summary.test.ts` にS8を追記
  - 手動疎通(Playwright 375×667、一時`/api/dev-login`+ローカルSupabase検証ユーザー2名。
    完了後にルート・ユーザー・データ・スクリーンショットを削除し`git status`で残存なし確認):
    - 今日/今週タブ・ダーク/ライトで「予定2件・着手1・未着手1・割り込み1件(30分)」を確認
    - 空データユーザーで「予定0件・割り込みなし」を確認
    - いずれも横スクロールなし(scrollWidth=375)、メタ行直下に配置
- 理由: 「今の期間の振り返りを深める」方向でのサマリー拡張(ユーザー選択)。
  算出済みデータの見せ方の追加のみでDB変更なし、P3-2スコープ外(按分・期間拡張等)は不変
- 結果: 単体353件(+7)・結合84件(+1)全合格。`npm run check`
  (typecheck+lint+test+build)合格を出力で確認
- 未解決・次にやること: PR作成しレビュー・マージへ。マージ後に開発計画のP5-3を「完了」へ更新
