# 2026-08-13

## (時刻不明) P10-1(提案経由の定期予定の自動学習補正)の調査・仕様策定

- やったこと:
  - ユーザー質問「提案で入れた予定は計測を継続していくことで再提案されますか?一度提案された
    予定を毎週登録した際に計測を継続することでズレが明確化されると考えています。使うほど
    予定の精度を向上させたい」を受け、まず現状調査(Exploreエージェント)。
    `lib/calendar/suggestions.ts` の `computeSuggestions` は直近4週間の実績を中央値
    クラスタリングして提案するだけの決定的な純粋関数で、「毎週にする」で定期予定を受け入れた
    後、実績とのズレをフィードバックして提案自体を賢くする仕組みは**存在しない**と判明。
    要件定義書 L100 の v2目玉その1「タスク種別ごとの見積もり係数学習・計画時のサジェスト」が
    まさにユーザーの想定に近い将来機能だが、開発計画には未起票の構想段階だった
  - AskUserQuestionで方向性を確認: 「新規タスクとして仕様化したい」を選択。続けて製品判断
    3点(効果=自動調整か表示のみか/対象範囲/v2目玉との位置づけ)を確認し、「自動で実績に
    近づく」「提案経由の予定のみ」「v2目玉の前倒し・具体化」で確定
  - Planエージェントで実装設計を調査・立案。**write スコープを持たないこのアプリでは
    「提案」経由の予定はGoogleカレンダーに書き戻されず、単発は`synced_events`
    (`source='app'`)、定期は`recurring_rules`+`synced_events`の二層構造で自前保存されている**
    ことを実装から確認(推測せず)。既存スキーマには「提案経由かどうか」を示す列がなく、
    `createAppEventAction`/`createRecurringRuleAction`はカレンダー画面・計測画面からの
    手動作成とも共用されているため、区別する列が学習対象の切り分けに必須と判明。
    単発予定は毎週`computeSuggestions`が最新実績から再計算する既存動作で実質的に
    最新実績へ寄るため学習の対象外でよいと整理し、対象は「毎週にする」で作られる
    `recurring_rules`(`origin='suggestion'`)に絞る設計とした
  - 残った未決事項4点(UI適用方式・停止トグルの要否・調整閾値・単発へのprovenance追加)を
    AskUserQuestionで確認。すべて推奨案(自動適用+お知らせ帯/停止トグルあり/
    3サンプル・7日おき・15分丸め/単発への列追加は見送り)で確定
  - plan modeで計画をまとめ、ユーザー承認を取得(2026-08-13)
  - `docs/開発計画.md` にPhase 10・P10-1を起票(状態=仕様書作成中)、v2以降テーブルの
    該当行に前倒しの注記を追記、「次に着手」冒頭を更新
  - `docs/specs/P10-1_提案経由予定の学習補正.md` を作成。P5-2/P5-7のフォーマットに倣い、
    データモデル(`recurring_rules`への`origin`/`last_learned_at`列追加。新テーブルは
    作らない)、`computeRuleAdjustments`/`applyRuleAdjustments`の擬似コード、UI
    (お知らせ帯・停止トグル)、テストシナリオ(単体T1〜T12・結合T13〜T19・
    コンポーネントT20〜T24・手動M1〜M3)を記載
- 理由:
  - CLAUDE.mdの開発方針(仕様書なし・ユーザー承認なしの実装禁止)に従い、コード実装には
    まだ着手しない。今回のゴールは仕様書と開発計画への起票まで
  - 新テーブルを作らず既存`recurring_rules`への列追加に留めたのは、カテゴリ横断の
    見積もり係数学習という本来のv2目玉本体はスコープ外とし、P5-2の「DB変更を最小に保つ」
    哲学を踏襲するため
  - 既存の`updateRecurringRule`(手動編集用)を流用せず専用の更新パスにするのは、
    ユーザーが明示的に削除した回(`recurring_exceptions`)を学習で勝手に復活させる事故を
    防ぐため
- 結果:
  - コード変更なし(ドキュメントのみ)。`docs/開発計画.md`・
    `docs/specs/P10-1_提案経由予定の学習補正.md` を追加・更新
- 未解決・次にやること:
  - ユーザーによる仕様書内容の最終確認・承認
  - 承認後、`spec-driven-dev` Skillに従いテストシナリオを展開しTDDで実装
    (マイグレーション→RLS確認→アプリコードの順。`db-migration` Skillも参照)

## (時刻不明) P10-1の実装(TDD)・完了

- やったこと:
  - ユーザーから仕様書の承認を得て、`spec-driven-dev` Skillに従い実装。仕様書ステータスを
    「承認済み」へ更新
  - `db-migration` Skillに従いマイグレーション作成: `recurring_rules` に `origin`
    (text, check制約 manual/suggestion, default 'manual')・`last_learned_at`(timestamptz)を
    追加。新規テーブル・RLS変更・GRANT変更なし。`npx supabase db reset`成功、`db diff`差分ゼロ
  - Red→Green→Refactorで実装:
    - `lib/calendar/rule-learning.ts` 新設。`computeRuleAdjustments`(純粋関数。中央値→15分丸めは
      `computeSuggestions`と同じ哲学)・`applyRuleAdjustments`(DB書き込み。`recurring_rules`更新+
      今日以降の`synced_events`のみ削除)・`disableRuleLearning`(停止トグル用)
    - 単体テスト12件(T1〜T12)を先に書きRedを確認 → 実装してGreen。`TZ=UTC`・
      `TZ=Pacific/Kiritimati`でも確認(R-1)
    - `lib/calendar/recurring-id.ts`に`RecurringRuleSummary.origin`/`lastLearnedAt`、
      `RuleAdjustmentNotice`型を追加。`lib/calendar/recurring.ts`の`RecurringRuleFormInput`に
      `origin`引数追加(既定'manual'。作成時のみ使用、更新では触らない)、`todayBoundary`をexport化
    - `plan-suggestions.tsx`の`makeWeekly`に`origin: "suggestion"`を追加。既存テスト(S16・S39)の
      完全一致アサーションを更新
    - 結合テスト9件(T13〜T19, T19b)を作成・全合格(ローカルSupabase)
    - UI: `components/rule-adjustment-banner.tsx`新設(お知らせ帯)。`recurring-rule-panel.tsx`に
      「自動調整を止める」トグル追加(`origin='suggestion'`のときのみ表示、成功時にトグル自体が消える)。
      コンポーネントテスト6件(T20〜T24)を先にRedで書いてから実装しGreen
    - `event-actions.ts`に`disableRuleLearningAction`追加。`lib/timer/entries.ts`に
      `fetchRuleLearningEntries`新設(現在時刻基準で直近43日の実績。取得失敗時は空配列)
    - `app/(app)/calendar/page.tsx`に配線: `origin='suggestion'`のルールが1件もなければ
      学習判定自体をスキップ。調整があった場合のみ再実体化し、`ruleAdjustments`をpropsで
      `CalendarView`へ渡す
  - **実機確認(Playwright、375px)で発見・修正したバグ**: ローカルSupabaseに提案経由ルール+
    実績を直接投入し`/calendar`を開くと、`recurring_rules`のDB値は正しく更新されるのに、
    画面上のカレンダーブロックの時刻が古いまま・お知らせ帯も出ないことを発見。原因は
    「学習補正の直後に同一リクエスト内で`materializeRecurringInstances`を再実行して
    再実体化する」設計が、Next.jsの**fetchリクエストメモ化**(同一レンダー内の同一fetch呼び出しを
    デデュープする挙動。`node_modules/next/dist/docs/`で仕様確認)により、更新直後でも
    `recurring_rules`への同一SELECTが更新前の内容を返してしまうため。自動テスト(Vitestは
    Next.jsのレンダーパイプライン外で動くため再現しない)では検出できず、DoDの375px実機確認
    ステップで初めて表面化した。`materializeRecurringInstances`に`prefetchedRules`引数を追加し、
    再実体化時はDBを読み直さず学習補正で判明済みの値をそのまま渡す設計に変更して解消
    (副次的に無駄な重複クエリも削減される)
  - 修正後、再度実機確認: お知らせ帯「『朝会』を実績にあわせて調整しました(開始
    09:00→09:15・所要 30分→45分)」表示、カレンダーブロックの時刻更新、定期予定編集パネルの
    停止トグルON→`origin`が`manual`に変わり以降対象外になることを、ライト/ダーク両方・375px幅で確認
- 理由:
  - `materializeRecurringInstances`の再実行時にDBを読み直さない設計にしたのは、Next.jsの
    fetchメモ化を無効化する(`cache: "no-store"`等)という広範囲に影響する変更を避け、
    「既に分かっている値を渡すだけで再クエリ自体が不要」という、より小さくデータ整合性の
    観点でも正しい修正を選んだため
- 結果:
  - 自動テスト: 単体+16件・結合+9件(単体742件・結合133件、全合格)。`npm run check`合格
    (typecheck/lint/test/build すべてEXIT=0)
  - 手動確認(Playwright、375px ライト/ダーク): お知らせ帯・停止トグルの表示と動作、DB反映を確認。
    横スクロールなし
- 未解決・次にやること:
  - 本番Supabaseへの`db push`(マイグレーションあり。ユーザー作業)
  - PR作成とユーザーによるマージ
