# 2026-08-07

## 07:40 P6-0 の完了(結合テストでRed→Greenを実証)

- 記録は 2026-08-06.md の「07:40」節に記載。ブランチ `feat/p6-0-summary-row-limit` にコミット済み
  (5511733)。PRのベースブランチは未決

## 08:00 P6-1 サーバー往復の集約(実装)

- やったこと:
  - 仕様書 `docs/specs/P6-1_サーバー往復の集約.md` 承認(2026-08-07)。ブランチ
    `feat/p6-1-server-roundtrips` を作成し、Red → Green で実装
  - **1-A**: `lib/supabase/session-user.ts` を新設し `getSessionUser(client)` を実装。
    `WeakMap<SupabaseClient, Promise<User|null>>` でクライアントインスタンス単位にメモ化する。
    `lib/supabase/server.ts` の `createClient` は `cache()` で包み、1リクエスト1インスタンスにした
  - **1-A-2**: `client.auth.getUser()` の直接呼び出しを `getSessionUser(client)` へ置換
    (`lib/timer/service.ts` 4 / `lib/calendar/app-events.ts` 3 / `lib/calendar/recurring.ts` 5 /
    `app/(app)` の layout・各page・各action)。`proxy.ts` はセッション更新という別の役割があるため対象外
  - **1-B**: `/calendar` の `profiles` 取得を `materializeRecurringInstances` と `Promise.all` で並列化。
    `redirect()` は例外を投げるため Promise.all の外(解決後)で判定する。
    `/settings` の `getUser → await searchParams → getGoogleRefreshToken` の3段直列も畳んだ
  - **1-C**: `recurring_exceptions` の取得に `occurrence_date` の期間フィルタ(前後1日バッファ)を追加。
    `materializeRecurringInstances` の戻り値を `RecurringRuleSummary[]` にし、`/calendar` の
    `fetchRecurringRules` 呼び出しを削除(同一テーブル・同一カラムの二重クエリを解消)。
    行→サマリ変換は `toRuleSummary` に抽出して `fetchRecurringRules` と共有
  - **1-D**: `createAdminClient` をモジュールスコープの lazy singleton に。
    `proxy.ts` の matcher から `/` `/pricing` `/privacy` `/terms` を除外(認証ページと `/api` は据え置き)
- 理由: `auth.getUser()` は毎回 `GET /auth/v1/user` へのHTTP往復でローカルキャッシュがない。
  `/calendar` 1リクエストで4回(proxy / layout / page / materialize)直列に往復しており、
  RSCではlayoutのawaitが解決するまでchildrenが始まらないため、そのままTTFBに積み上がっていた
- 設計上の判断:
  - **React `cache()` 単独にしなかった**。前日の実測どおり `cache()` はリクエストコンテキスト外で
    素通しするため、「1リクエストで1回」を vitest で固定できない。WeakMap なら RSC・Server Action・
    Route Handler・テストのすべてで同じ挙動になり、回帰防止テストが書ける
  - **`getSessionUser` を `lib/supabase/server.ts` に置かなかった**。同ファイルは `next/headers` を
    importしており、そこに置くと `lib/` のサービス層全体がNextのリクエストAPIに依存してしまう。
    `next/headers` に依存しない別モジュールに分離した
  - 既存のDI(テストがモッククライアントを注入する設計)を維持するため、`getSessionUser` は
    引数で client を受け取る形にした
- 結果:
  - 単体/コンポーネント: **564件全合格**(P6-0時点の528 → +36)。`npm run check` 合格
  - 結合: **103件全合格**(P6-0時点の99 → +4)
  - 途中、`npm run check` の一括実行でコンポーネントテスト7件がタイムアウトしたが、
    1ファイルあたり900秒という実行時間から実行環境の負荷と判断。`tests/components/` を
    単独実行して183件全合格を確認し、その後クリーンな状態で `npm run check` を再実行して
    `EXIT=0` を確認した(回帰ではない)
  - 効果の実測(結合テスト S16 として固定): 未使用のクライアントに対して
    `/calendar` 相当の3箇所から `getSessionUser` を並行で呼び、**実クライアントの
    `auth.getUser` が1回しか呼ばれない**ことを確認。往復 3 → 1
  - `/calendar` の `recurring_rules` クエリ 2回 → 1回、`profiles` の直列1往復を並列化
- 未解決・次にやること:
  - `getClaims()`(ローカルJWT検証)への移行は、残る1回の往復も消す施策として将来評価する

## 14:20 PRのスタック整理(P6-0 / P6-1)

- やったこと: `origin` を fetch したところ、`docs/p5-9-complete`(9163797)の内容が
  **すでに main にマージ済み**(`e3f72b4`。`git diff` が空)と判明。当初想定していた
  「PRを3本積む」構成が不要になったため、両ブランチを `main` 起点にリベースし直した
- 結果: 重複コミットはリベース時に git が自動スキップ。リベース後に単体564件の合格を確認。
  PR #48(P6-0 → main)、PR #49(P6-1 → feat/p6-0)を作成
- 未解決: 本番Supabaseの `Max rows` 実値の確認(ユーザー作業)は未完のまま

## 14:30 P6-2 データ増に耐えるクエリとインデックス(仕様策定〜実装)

- やったこと:
  - **承認済みプランの設計を変更**して仕様書 `docs/specs/P6-2_...md` を作成・承認取得(シナリオ15件)
  - `db-migration` Skill に従い マイグレーション → RLS → アプリコード の順で実装
  - マイグレーション `20260807143501_range_index_and_policy_roles.sql`:
    - `btree_gist` 拡張、`synced_events` / `time_entries` に `span tstzrange` 生成列、
      `using gist (user_id, span)` のインデックス
    - 補助インデックス3本(`(user_id, source, start_at)` / `google_event_id text_pattern_ops` /
      `pro_interest_events (event_type, created_at desc)`)
    - 全18ポリシーを `to authenticated` 付きで再定義(条件は既存と同一)
  - アプリ側: `fetchSyncedEventsInRange` / `fetchTimeEntriesInRange` /
    `app/api/calendar/sync/route.ts` の2箇所を `.overlaps("span", toRangeLiteral(range))` へ。
    `toRangeLiteral` は `lib/google/sync-range.ts` に追加
- 理由(設計変更の根拠): 当初プランの2-Aは「予定の最大継続時間を7日と仮定して `start_at` に
  下限を足す」案だったが、**7日を超える予定が黙って結果から消える**。P6-0で直したばかりの
  バグとまったく同じ種類の欠陥を持ち込むため取り下げ、仮定を置かない範囲型+GiSTに変更した
- **実装中に実DBで欠陥を検出・修正**: 生成列の評価がCHECK制約より先に走るため、
  `end_at < start_at` の不正データで `tstzrange()` が data_exception(22000)を投げ、
  本来の check_violation(23514)を覆い隠していた(既存の結合テストS7が検出)。
  生成列の `case` で不正な組み合わせを `null` に倒し、拒否はCHECKに任せるようにした
- 結果:
  - `npx supabase db reset` 成功、`npx supabase db diff` は `No schema changes found`
  - 単体/コンポーネント: **565件全合格**(P6-1時点の564 → +1)。`npm run check` は `EXIT=0`
  - 結合: **115件全合格**(103 → +12)
  - 効果の実測(本番スキーマ・26,000行 ≒ 3年ぶん):
    ```
    BEFORE: Seq Scan on synced_events (rows=504)
              Rows Removed by Filter: 25496
            Execution Time: 2.387 ms
    AFTER:  Bitmap Index Scan on synced_events_user_span_idx (rows=504)
            Execution Time: 0.090 ms      ← 約26倍
    ```
    S11として「GiSTインデックスを使い `Seq Scan` にならないこと」をテストで固定した
- 未解決・次にやること: P6-3(クライアントバンドル・レンダリング)が未着手

## 23:50 本番Supabaseへのマイグレーション適用(db push)

- やったこと:
  - PR #48 / #49 / #50 がマージ済みであることを確認し、`main` を最新化
  - `npx supabase db push --dry-run` で対象を確認してから `npx supabase db push` を実行
- 理由: ユーザー指示。P5-1 / P4-4 のぶんが3週間以上未適用のまま残っており、
  本番で定期予定機能と料金ページの計測が動作していなかった
- 結果:
  - 3本すべて適用成功:
    - `20260713035812_pro_interest_events.sql`(P4-4。未適用だと `/pricing` の計測記録が失敗していた)
    - `20260713220814_recurring_events.sql`(P5-1。未適用だと定期予定機能が動作しなかった)
    - `20260807143501_range_index_and_policy_roles.sql`(P6-2)
  - 適用後の `db push --dry-run` は `{"upToDate":true,"migrations":[]}`
  - `db diff --linked` に P6-2 の生成列・GiSTインデックス・ポリシーロールに関する差分は出ておらず、
    本番スキーマがマイグレーションと一致していることを確認
- **本番でのみ見つかった要対処事項(セキュリティ)**:
  - `db diff --linked` により、**本番では `anon` ロールに全テーブルの DELETE/INSERT/SELECT/UPDATE が
    付与されている**ことが判明(`google_tokens` / `pro_interest_events` を含む)。
    ローカルでは付与されていない(`REFERENCES,TRIGGER,TRUNCATE` のみ)ため、リポジトリ側からは見えなかった
  - 原因は本番プロジェクト側の `ALTER DEFAULT PRIVILEGES ... TO anon`。
    新規テーブルにも自動で anon 権限が付く設定になっている
  - **現時点で情報漏洩は発生しない**。`google_tokens` / `pro_interest_events` は
    RLS有効・ポリシーゼロで、DML は RLS が全拒否するため。GRANT だけでは RLS を迂回できない
  - ただし CLAUDE.md が定める「RLS有効・ポリシーゼロ・GRANTなしの三重防御」は本番では
    二層になっている。将来 permissive なポリシーが追加されたり RLS が外れた瞬間に
    anon が全操作可能になるため、`revoke` するマイグレーションを別項目として起票すべき
  - あわせて本番には `ensure_rls` イベントトリガ(新規テーブルへの自動RLS有効化)と
    `pg_net` 拡張が存在する。どちらもSupabase側の管理対象で、ローカルには無い。**触らないこと**
- 未解決・次にやること:
  - 上記の anon GRANT 剥奪を Phase 6 の項目として起票し、仕様承認を得てから対応する
  - 本番Supabaseの `Max rows` 実値の確認(P6-0 の前提。未確認のまま)
