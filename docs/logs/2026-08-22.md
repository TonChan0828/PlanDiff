# 2026-08-22

## 15:10 週表示の日付選択と編集導線の修正(P11-1)

- やったこと:
  - ユーザー報告「当日以外の予定を編集できるようにしたい」をヒアリングし、
    「週間のカレンダー表示時に任意の日にちの予定を操作できない」ことだと特定
  - コードベース調査(並行Explore調査3回): 実績・アプリ内予定の編集ロジック自体には
    日付制限がないこと、唯一の日付依存の制約は定期予定「繰り返し全体」編集(過去回は
    意図的に不変)であることを確認。その上でユーザーへの追加ヒアリングで、実際の原因が
    「週表示に選択日を切り替えるUIがない」ことだと判明(3回目のExplore調査で特定)
  - `docs/specs/P11-1_週表示の日付選択と編集導線.md` を作成・承認を得た
  - TDDで実装: `tests/components/calendar-view-week-select.test.tsx` を新設し
    S1〜S4(Red確認)→ `components/calendar-view.tsx` の週表示日ヘッダーを`<div>`から
    `<button>`化(`WeekStrip`と同じ3状態スタイル+`aria-pressed`/`aria-current`)→Green
  - 手動疎通: Dockerを起動しローカルSupabaseを立ち上げ、一時的な`/api/dev-login`ルート
    (本番ガード付き)+検証ユーザー1名+アプリ内予定1件・確定済み実績1件(いずれも
    「今日ではない日」)を投入し、Playwrightで375px(ライト/ダーク)を実機確認
  - 手動疎通中にタップターゲットが42px(基準44px未達)であることを発見し、`min-h-11`で修正
  - 検証ユーザー・一時ルート・スクリーンショットを削除し、`npm run check`を再実行して合格を確認
- 理由:
  - このリポジトリの`spec-driven-dev`/`ui-quality` Skillの運用に従い、仕様書承認→TDD→
    実機確認の順で進めるため
- 結果:
  - `npm run check` 合格(typecheck/lint/test 748件全合格/build)。R-1(TZ=UTC・
    TZ=Pacific/Kiritimati)も確認済み
  - 手動疎通: 週表示で他日の日ヘッダーをタップ→選択状態に切替→コンテキストパネルに
    その日の予定・実績が表示→編集パネルが正しい初期値で開くことを確認。横スクロール0px
- 未解決・次にやること:
  - なし(P11-1は完了)
