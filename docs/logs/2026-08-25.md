# 2026-08-25

## P13-1(計測しっぱなしの検知とPush通知) — Task 7: 仕上げ(375px確認・ドキュメント・残タスクの明文化)

- やったこと:
  - Task 1〜6(マイグレーション / 純粋ロジック / Push送信 / 購読API / cron / Service Worker と
    設定UI)の実装が完了した状態を受け、最終タスクとして**検証とドキュメントのみ**を実施
    (機能コードは変更していない)
  - `.env.example` に Web Push 用の環境変数3本をプレースホルダで追記:
    `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `CRON_SECRET`
    (レビュー指摘「運用初期のセットアップ漏れリスク」への対応)
  - `npm run check` を実行し出力と終了コードを確認:
    typecheck・lint 合格、`npm test` **128ファイル / 791件 全合格**、`next build` 成功、**EXIT=0**
  - `npx supabase start` 済みのローカルSupabaseで `npm run test:integration` を実行:
    **25ファイル / 152件 全合格、EXIT=0**
  - **R-1(日時テストのTZ確認)**を実施:
    - `TZ=UTC npm test` → 128ファイル/791件全合格、EXIT=0
    - `TZ=Pacific/Kiritimati npm test`(UTC+14)→ 128ファイル/791件全合格、EXIT=0
  - `npx supabase db reset` → EXIT=0、直後の `npx supabase db diff` →
    `"No schema changes found"`(意図しない差分なし)
  - **375px幅の実機ブラウザ確認**(Playwright、`mcp__plugin_playwright_playwright__*`):
    - ローカルSupabaseの Admin API(`/auth/v1/admin/users`)でテストユーザーを1件作成し、
      `npm run dev` を起動して `/login` → メール/パスワードでログイン → `/settings` を
      375×667で確認。**通知の許可ダイアログは一切出していない**(押していない)。
      ブラウザAPI(`Notification.permission` / `window.PushManager` /
      `navigator.serviceWorker.register`)をPlaywright経由でJSから上書きして、
      実際に許可ダイアログを介さずに4状態+`unsupported`を再現し、クライアント側の
      ルーティング遷移(remount)で状態を切り替えて確認した
    - 確認できたこと(実測値):
      - 横スクロール: 全状態で `document.documentElement.scrollWidth === window.innerWidth === 375`
        (0px)
      - `unsupported`(「この環境では通知を利用できません」)/ `iosNeedsHomeScreen`(長文)/
        `blocked`(「ブラウザの設定で通知がブロックされています…」)/ `disabled`(未許可。
        「通知を有効にする」ボタン)/ `enabled`(「この端末で有効」+「無効にする」ボタン)
        の5状態すべてで文言が折り返され、はみ出しなし
      - `iosNeedsHomeScreen` の長文(「iPhone・iPadでは、ホーム画面に追加したPlanDiffからのみ
        通知を受け取れます。共有メニューの「ホーム画面に追加」を実行してから、もう一度
        お試しください」)をスクリーンショットで目視確認。ライト/ダーク両方で確認
      - タップターゲット実測: 「通知を有効にする」44px、「無効にする」44px(`min-h-11` 通り)
      - コンソールエラー0件
    - 後始末: 作成したテストユーザーはAdmin APIで削除、スクリーンショットと
      `.playwright-mcp/` は削除、開発サーバーは停止済み。リポジトリの変更は
      ドキュメントと `.env.example` のみ
  - `docs/specs/P13-1_計測しっぱなしの検知とPush通知.md` を更新:
    - ステータスを「実装完了(2026-08-25)」に変更
    - 「検証(手動)」のうち実際に確認できた項目にのみチェック
      (`npm run check` / `db reset`+`db diff` / 375px確認)。
      **実機でのPush到達確認・iOS実機確認は未実施のまま未チェックで残した**
    - 「実装後の補足」節を追加し、Task 1〜6レビューで見つけて修正した不具合3件・
      テスト規模の実測値・R-1確認結果を明記
    - 「残タスク」に「実機でのPush到達確認」「本番でのcron実行確認」を明記
  - `docs/開発計画.md` を更新:
    - P13-1の状態を「承認待ち」→「完了」に変更、完了メモを追記
    - 「次に着手」セクションの項目0を、実装完了・残るのはユーザー作業のみである旨に書き換え
    - `**最終更新**` 行を更新
- 理由:
  - CLAUDE.md「確認していないことを確認したと書かない」を最優先とし、実機でのPush到達確認・
    iOS実機確認は環境(実デバイス)が要るため今回のタスクの範囲外と判断し、未チェックのまま
    「残タスク」に明記した
  - 375px確認は認証必須の画面のため、Playwright + ローカルSupabaseのAdmin APIでテストユーザーを
    作成する経路を取った。実ブラウザでは `PushManager` が実在するため素の状態は「未許可」だが、
    ブラウザAPIをJSで上書きすることで許可ダイアログを一切出さずに残り3状態(+`unsupported`)を
    安全に再現できた
- 結果:
  - `npm run check` / 結合テスト / R-1(TZ×2)/ `db reset`+`db diff` / 375px確認、
    いずれも実施し出力を確認。すべて合格
  - コード変更なし。変更点は `.env.example` とドキュメント3点(本ログ・仕様書・開発計画)のみ
- 未解決・次にやること(ユーザー作業。仕様書「残タスク」と同一):
  - `npx web-push generate-vapid-keys` で鍵を生成し、Vercelに
    `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `CRON_SECRET` を登録する
    (ローカル `.env.local` にも同じ3本を追加する)
  - 本番Supabaseへ `npx supabase db push`(テーブル追加+列追加のため必須)
  - 本番デプロイ後、Vercelダッシュボードの Cron Jobs にジョブが登録されたことを確認する
  - 実機(Android Chrome / iOSはホーム画面追加したPWA)でPush通知の到達を確認する
