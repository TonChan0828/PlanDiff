# 2026-08-08

## 00:00 P6-3 クライアントバンドルとレンダリングコストの削減(仕様策定〜実装)

- やったこと:
  - 仕様書 `docs/specs/P6-3_クライアントバンドルとレンダリング.md` を作成・承認取得(シナリオ14件+M1〜M3)。
    **書く前に `npm run build` でベースラインを実測**し、仕様書に before 値を記録した
  - 仕様どおり、最初に「vitest で `next/dynamic` が解決できるか」を使い捨てテストで確認 → 解決できた
    ため 3-F を実施(解決できなければ落とす判断だった)
  - **3-A**: `lib/ui/ja-locale.ts` を新設し、`date-fns/locale` の `ja` を差し替え
    (`calendar-view` / `track-view` / `calendar-context-panel` / `lib/summary/format.ts` の4箇所)
  - **3-B**: `next.config.ts` に `optimizePackageImports: ["date-fns", "lucide-react"]`
  - **3-C**: `lib/calendar/layout.ts` に `hasEventOnDay` を新設し、`rangeIsEmpty` の捨て計算を置換。
    判定条件は `intersectsDay` として `layoutDayEvents` と共有し、結果が常に一致することを保証
  - **3-D**: `DayColumn` の `blocks` / `actualBlocks` / `planByEventId` / `gaps` を `useMemo` 化。
    親の `actualInputs` も `useMemo` 化
  - **3-E**: `CalendarContextPanel` の filter+sort を `useMemo` 化、`MediaQueryList` をモジュール
    スコープにキャッシュ
  - **3-F**: 編集系パネル5種を `next/dynamic` で遅延ロード(`calendar-view` 5種 / `track-view` 2種)
  - **3-G**: sticky バー2箇所から `backdrop-blur` を撤去
- 理由: P6-1(サーバー往復)・P6-2(DBクエリ)でサーバー側は片付いたため、残るクライアント側に着手した
- **仕様から方針を変えた点(重要)**:
  - 仕様では 3-D の手順1を「`now` を安定化する」としていたが、`useMemo([hydrated])` で固めると
    現在時刻ラインが**ハイドレーション時刻で永久に固定**され、今より悪化することに実装中に気づいた。
    そこで `now` は据え置き、代わりに **`actualInputs` の基準時刻を分単位に丸めて**
    参照を安定させた(ブロック高さの差は1分未満=1px未満)。`blocks` の `useMemo` は `now` に
    依存させていないため、現在時刻ラインの更新とレイアウトのメモ化を両立できる
  - 同じ理由で `useCallback` + `React.memo`(仕様の手順3・5)は見送った。ハンドラの依存の連鎖が
    1472行のコンポーネント全体に及び、stale closure のリスクに対して追加の効果が小さいため。
    重い計算は `DayColumn` 内の `useMemo` で既に止まっている
  - 既存のパネル系テスト13件を `getBy*` → `findBy*` に変更した。遅延ロードは実際に非同期に
    なったための適合であり、テストの無効化ではない
- **想定と実測が食い違った点**: 3-A の効果は想定よりかなり小さかった。
  `ja` の日本語文字列は消えた(`日曜日: 0` / `秒未満: 0` を実測で確認)が、
  `matchPatterns` / `formatDistance` は残る。これは date-fns の `format()` が
  **既定ロケール(enUS)を静的 import している**ためで、`ja` を外しても消えない。
  3-A 単独の効果は raw −5,097 / gz −1,338 に留まった。
  残りの64KBを削るには `format()` 自体を自前実装に置き換える必要があり、本仕様のスコープ外
- 結果:
  - **バンドル(First Load JS、非圧縮)**:
    ```
    /calendar   668,915 → 643,392   -25,523  (-3.8%)
    /track      623,451 → 587,786   -35,665  (-5.7%)
    ```
    他ルートは変化なし(編集パネル・カレンダーを含まないため)
  - **71,599バイトの重複チャンク2本(/calendar と /track に別々に配信)は解消**
  - 単体/コンポーネント: **601件全合格**(P6-2時点の565 → +36)。`npm run check` は `EXIT=0`
  - 結合: **115件全合格**(変化なし。クライアント側の変更のため)
- 未解決・次にやること:
  - `format()` の自前実装による date-fns 本体(64KB)の削減は将来検討
  - P6-4(Google同期経路)は凍結解除待ち、P6-5(本番の anon GRANT 剥奪)が未着手

## 09:20 P6-3 の375px手動確認(M1〜M3)

- やったこと:
  - 一時的な `/api/dev-login`(`NODE_ENV !== "development"` で404を返す本番ガード付き)と
    ローカルSupabaseの検証ユーザー1名+予定4件・実績3件を作成し、Playwright 375×667 で確認
  - 過去(P2-3 / P3-1 等)と同じ手順。確認後、一時ルート・検証ユーザー・スクリプト・
    スクリーンショットをすべて削除し、`git status` が空であることを確認済み
- 結果(いずれも実測値):
  - **M1 レイアウト**: `/calendar` 日表示・週表示、`/track`、`/summary` のすべてで
    **横スクロール 0px**(375px幅)。週表示は7列が収まり、曜日ラベルは 月・火・水・木・金・土・日
  - **最小localeの実地確認**: `/summary` のラベルが `2026年8月8日(土)` と正しく描画された
    (2026-08-08 は実際に土曜)。3-A の置換が実環境で機能していることを確認
  - **M2 backdrop-blur撤去後の視認性**: sticky バーは `backdropFilter: none` になり、
    背景95%不透明(ライト `oklab(.99…/0.95)` / ダーク `oklab(.217…/0.95)`)+境界線+影で
    背景との分離を維持。ライト/ダーク両方で確認
  - **M3 遅延ロードとフォーカストラップ**: 「予定を追加」で `role="dialog"`(aria-label「予定を追加」)が
    表示され、**フォーカスがダイアログ内(閉じるボタン)へ移動**。パネル幅375px・はみ出しなし
  - 併せて集計表示も正常(「+15分 (+17%)」「着手 予定より +15分遅れ」「-1時間 (-50%)」「未着手」)
  - タップターゲット44px未満の要素(再計測▶ 24px、日/週トグル 42×44、ワードマーク)は
    **いずれもP6-3で触っていない既存要素**。再計測▶はP5-4で意図的に小さくしている箇所
- 未解決・次にやること: P6-3 のPR作成。P6-5(本番の anon GRANT 剥奪)が未着手

## 10:00 CI失敗の調査と修正(TZ依存テスト)

- やったこと:
  - PR #52 のCIが失敗。`gh run view --log-failed` で
    `tests/lib/calendar/layout.test.ts:110` の `expected false to be true` を特定
  - 原因は**私が書いたテストのTZ依存**。`day = new Date("2026-08-03T00:00:00+09:00")`
    (= 2026-08-02T15:00Z)に対しイベントを `2026-08-03T00:00:00Z` 固定で書いていた。
    JSTでは `startOfDay(day)` が 08-03 00:00 JST になり範囲内だが、
    **UTCでは `dayEnd` が 08-03 00:00Z ちょうどになり `start < dayEnd` が false** になる
  - 既存テストの流儀(`new Date(2026, 6, 7, h, m)` によるローカルTZ構築)に合わせて全面的に書き直し、
    日の境界ちょうどのケース(0:00開始 / 0:00終了)も `layoutDayEvents` との一致で固定するテストを追加
  - **CLAUDE.md の「バグ由来ルール」に R-1 を追記**。TZ起因のCI失敗はこれで3回目
    (P5-2 PR #28 / D-5 2026-07-19 / 今回)で、CLAUDE.md の「同じバグを3回出したらルール化する」に該当
- 理由: 開発機はJST・CIはUTCで走るため、ローカルのpre-commitフックでは検出できない。
  ルール化しないと4回目が起きる
- 結果:
  - `tests/lib/calendar/layout.test.ts` は **TZ=UTC / America/New_York(UTC-4) /
    Pacific/Kiritimati(UTC+14) の3TZで11件合格**
  - **全体を `TZ=UTC` で実行して602件全合格**(CIと同条件)。他にTZ依存の持ち込みがないことを確認
- 未解決・次にやること: PR #52 の再CI確認

## 11:30 P6-5 テーブル権限の最小化(仕様策定〜実装)

- やったこと:
  - 仕様書 `docs/specs/P6-5_anon権限の剥奪.md` を作成・承認取得(シナリオ8件)
  - `db-migration` Skill に従い マイグレーション → 検証 の順で実装。
    `20260808024737_revoke_anon_table_grants.sql`:
    - `revoke all on all tables in schema public from anon`
    - `authenticated` も一度すべて剥奪し、必要な5テーブルぶんだけ付け直す
    - `alter default privileges ... revoke all on tables/sequences from anon, authenticated`
  - CLAUDE.md の DB/RLS 規約に3項目を追記(新規テーブルへのgrant明示 / anonに権限を与えない /
    ポリシーへの `to authenticated` 明示)
- 理由: 2026-08-07 の本番 db push で `db diff --linked` により、本番の `anon` に
  全テーブル(`google_tokens` 含む)のDMLが付与されていることが判明した。
  さらに調査でローカル・本番とも `anon`/`authenticated` に **TRUNCATE**(RLS対象外)が
  付いていることも判明。**現時点ではどの経路からも悪用できない**(RLSがポリシーゼロで全拒否、
  PostgRESTはTRUNCATEを公開せず、anonにDBログインも無い)が、
  防御が一段減っており、既定権限により新規テーブルにも自動で付くため直した
- **実装中に判明した制約(仕様書に追記済み)**:
  - 既定権限は**作成ロールごと**に定義され、public のテーブルには2組あった。
    `postgres` 所有(=マイグレーションが作るテーブルに適用)と `supabase_admin` 所有。
    5-C が消せるのは前者だけで、後者は `ALTER DEFAULT PRIVILEGES FOR ROLE supabase_admin` に
    メンバーシップが要るため変更できず、Supabase管理としてスコープ外。
    マイグレーションは postgres として走るため**実害はない**
  - `service_role` の既定は変更前から `Dxtm`(DMLなし)。**マイグレーション適用前後で
    `pg_default_acl` を実測し、5-C による変更がないことを確認**した
    (一度マイグレーションを退避して `db reset` し、前後比較した)
- 結果:
  - 適用後の権限: **`anon` は0件**、`authenticated` は profiles=SELECT,UPDATE /
    synced_events・time_entries・recurring_rules・recurring_exceptions=4種DMLのみ。
    `TRUNCATE`/`REFERENCES`/`TRIGGER` と `google_tokens`/`pro_interest_events` への権限が消えた
  - `npx supabase db reset` 成功、`npx supabase db diff` は `No schema changes found`
  - 結合: **123件全合格**(115 → +8)
- 未解決・次にやること:
  - **本番への `db push` はユーザー作業**。適用後に `db diff --linked` で
    `anon` の GRANT が消えたことを確認する
  - 本番の既定権限は `GRANT ... TO anon` を含むため、本マイグレーションで解消される見込み

## 12:30 PR #53 のCI失敗の調査(P6-3由来の潜在フレーク)

- やったこと:
  - PR #53(P6-5)のCIが失敗。ログを確認すると失敗したのは
    `tests/components/track-view.test.tsx` の「予定にする」で
    `Unable to find an accessible element with the role "dialog"`。
    **P6-5(SQL・ドキュメントのみ)とは無関係**で、P6-3 の `next/dynamic` 化で
    私が作り込んだ潜在フレークがCIの負荷で表面化したものと判断
  - 目視のgrepでは網羅できないため、**`next/dynamic` の解決を60ms遅らせる一時的な
    vitest構成を作り、フレークを決定的に再現**させた
- 理由: 「ローカルで通る」は遅延ロードが即座に解決しているだけで、
  CIの負荷次第で落ちる。1件ずつ直しても他が残る可能性が高い
- 結果:
  - 遅延注入により、通常実行では出ない**6件**を検出・修正:
    - `track-view.test.tsx`(CIが検出した1件)
    - `calendar-view-edit.test.tsx` 削除フロー2件
    - `calendar-view-recurring.test.tsx`「この予定のみ」2件
    - `track-view-running-start.test.tsx` S5 2件
  - 修正後、**遅延注入下で190件全通過**。通常構成・`TZ=UTC` でも602件全通過
  - 検証用の一時ファイル(`tests/slow-dynamic.setup.ts` / `vitest.slow.config.ts`)は削除済み
- 未解決・次にやること: PR #53 の再CI確認

## 14:00 P6-5 の本番適用と、検証で見つかった取りこぼしの修正(5-D)

- やったこと:
  - PR #53 マージ後、`main` を最新化して `npx supabase db push --dry-run` → `db push` を実行
  - 適用後に `db diff --linked` で `anon` の GRANT が消えたことを検証
- 結果(本番):
  - `20260808024737_revoke_anon_table_grants.sql` 適用成功。`db push --dry-run` は `upToDate:true`
  - **テーブルへの `anon` GRANT は全て消えた**(適用前は `google_tokens` を含む全7テーブルに
    `GRANT DELETE, INSERT, SELECT, UPDATE ... TO anon` があった)
  - TABLES / SEQUENCES の既定権限からも `anon` / `authenticated` が消えた。
    `service_role` の既定は意図どおり残存
- **検証で判明した取りこぼし(自分の設計漏れ)**:
  - `ALTER DEFAULT PRIVILEGES ... GRANT ALL ON ROUTINES TO anon / authenticated` が残っていた。
    P6-5 本体の 5-C が **TABLES と SEQUENCES しか対象にしていなかった**ため
  - 実害はない(自前関数はすべて `private` スキーマにあり `anon`/`authenticated` に USAGE がない。
    `public` にあるのは btree_gist 拡張の関数と Supabase 管理の `rls_auto_enable()` のみ)が、
    「新規オブジェクトに自動で権限が付かないようにする」という趣旨に反するためユーザー承認を得て追補
  - 追補マイグレーション `20260808050650_revoke_routine_default_privileges.sql` を作成。
    既定権限のみを対象とし、**既存の関数は触らない**(拡張所有 / Supabase管理のため)
  - ローカルの `postgres` 所有 ROUTINES 既定はもともと `{postgres=X/postgres}` で anon を含まず、
    このマイグレーションはローカルでは実質no-op。**本番でのみ効果がある**
- 結果(追補):
  - `db reset` 成功、`db diff` は `No schema changes found`
  - 結合: **124件全合格**(123 → +1。S9=既定権限が関数にも anon/authenticated を含まないこと)
  - CLAUDE.md の DB/RLS 規約に「public に関数を追加する場合は `grant execute` を明示。
    自前関数は原則 `private` に置く」を追記
- 未解決・次にやること:
  - 追補マイグレーションの**本番への `db push`**(PRマージ後)
  - 本番Supabaseの `Max rows` 実値の確認(P6-0 の前提。未確定のまま)

## 16:00 Phase 6 の完了反映と、次の着手先の棚卸し

- やったこと:
  - 本番DBの適用状況を `npx supabase db push --dry-run` で確認
  - `docs/開発計画.md` が実態から遅れていたため更新(P6-3・P6-5 が「実装完了・PR作成待ち」のままだった)
- 理由: PR #52/#53/#54 がすべてマージ済みで、計画表だけが取り残されていた(DoD 5違反の状態)
- 結果:
  - 本番は `upToDate:true`。P6-5 追補(`20260808050650_revoke_routine_default_privileges.sql`)まで適用済み
  - P6-3 を「完了」に更新。備考に PR #52 と、PR #53 のCIで表面化した遅延ロード起因のフレーク6件の
    後日修正を追記
  - P6-5 を「完了」に更新。本番 `db push` 適用済み・追補(PR #54)まで反映
  - 「次に着手」を書き直し。**コード側の着手待ちキューは空**であることを明記し、
    候補を P4-6 / Google連携の凍結解除 / 新規機能拡張 / ユーザー作業 / P3-3 に整理
- 未解決・次にやること:
  - 次の着手先の決定(ユーザー判断)。特に**Google連携の凍結解除はOAuth審査のリードタイムが長い**ため、
    やるなら早めに判断したい(解除すると P0-5 → P6-4、P1-2残、P1-3残がセットで動く)
  - ユーザー作業(実装をブロックしない): 本番Supabaseの `Max rows` 実値確認(P6-0の前提)、
    認証設定の Redirect URL 追加とメール確認必須化(P1-3残)
