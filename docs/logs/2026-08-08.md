# 2026-08-08

## 00:00 P6-3 クライアントバンドルとレンダリングコストの削減(仕様策定〜実装)

- やったこと:
  - 仕様書 `docs/specs/P6-3_クライアントバンドルとレンダリング.md` を作成・承認取得(シナリオ14件+M1〜M3)。
    **書く前に `npm run build` でベースラインを実測**し、仕様書に before 値を記録した
  - 仕様どおり、最初に「vitest で `next/dynamic` が解決できるか」を使い捨てテストで確認 → 解決できた
    ため 3-F を実施(解決できなければ落とす判断だった)
  - **3-A**: `lib/ui/ja-locale.ts` を新設し、`date-fns/locale` の `ja` を差し替え
    (`calendar-view` / `track-view` / `calendar-context-panel` / `lib/summary/format.ts` の4箇所)
  - **3-B**: `next.config.ts` に `optimizePackageImports: ["date-fns", "lucide-react"]`
  - **3-C**: `lib/calendar/layout.ts` に `hasEventOnDay` を新設し、`rangeIsEmpty` の捨て計算を置換。
    判定条件は `intersectsDay` として `layoutDayEvents` と共有し、結果が常に一致することを保証
  - **3-D**: `DayColumn` の `blocks` / `actualBlocks` / `planByEventId` / `gaps` を `useMemo` 化。
    親の `actualInputs` も `useMemo` 化
  - **3-E**: `CalendarContextPanel` の filter+sort を `useMemo` 化、`MediaQueryList` をモジュール
    スコープにキャッシュ
  - **3-F**: 編集系パネル5種を `next/dynamic` で遅延ロード(`calendar-view` 5種 / `track-view` 2種)
  - **3-G**: sticky バー2箇所から `backdrop-blur` を撤去
- 理由: P6-1(サーバー往復)・P6-2(DBクエリ)でサーバー側は片付いたため、残るクライアント側に着手した
- **仕様から方針を変えた点(重要)**:
  - 仕様では 3-D の手順1を「`now` を安定化する」としていたが、`useMemo([hydrated])` で固めると
    現在時刻ラインが**ハイドレーション時刻で永久に固定**され、今より悪化することに実装中に気づいた。
    そこで `now` は据え置き、代わりに **`actualInputs` の基準時刻を分単位に丸めて**
    参照を安定させた(ブロック高さの差は1分未満=1px未満)。`blocks` の `useMemo` は `now` に
    依存させていないため、現在時刻ラインの更新とレイアウトのメモ化を両立できる
  - 同じ理由で `useCallback` + `React.memo`(仕様の手順3・5)は見送った。ハンドラの依存の連鎖が
    1472行のコンポーネント全体に及び、stale closure のリスクに対して追加の効果が小さいため。
    重い計算は `DayColumn` 内の `useMemo` で既に止まっている
  - 既存のパネル系テスト13件を `getBy*` → `findBy*` に変更した。遅延ロードは実際に非同期に
    なったための適合であり、テストの無効化ではない
- **想定と実測が食い違った点**: 3-A の効果は想定よりかなり小さかった。
  `ja` の日本語文字列は消えた(`日曜日: 0` / `秒未満: 0` を実測で確認)が、
  `matchPatterns` / `formatDistance` は残る。これは date-fns の `format()` が
  **既定ロケール(enUS)を静的 import している**ためで、`ja` を外しても消えない。
  3-A 単独の効果は raw −5,097 / gz −1,338 に留まった。
  残りの64KBを削るには `format()` 自体を自前実装に置き換える必要があり、本仕様のスコープ外
- 結果:
  - **バンドル(First Load JS、非圧縮)**:
    ```
    /calendar   668,915 → 643,392   -25,523  (-3.8%)
    /track      623,451 → 587,786   -35,665  (-5.7%)
    ```
    他ルートは変化なし(編集パネル・カレンダーを含まないため)
  - **71,599バイトの重複チャンク2本(/calendar と /track に別々に配信)は解消**
  - 単体/コンポーネント: **601件全合格**(P6-2時点の565 → +36)。`npm run check` は `EXIT=0`
  - 結合: **115件全合格**(変化なし。クライアント側の変更のため)
- 未解決・次にやること:
  - `format()` の自前実装による date-fns 本体(64KB)の削減は将来検討
  - P6-4(Google同期経路)は凍結解除待ち、P6-5(本番の anon GRANT 剥奪)が未着手

## 09:20 P6-3 の375px手動確認(M1〜M3)

- やったこと:
  - 一時的な `/api/dev-login`(`NODE_ENV !== "development"` で404を返す本番ガード付き)と
    ローカルSupabaseの検証ユーザー1名+予定4件・実績3件を作成し、Playwright 375×667 で確認
  - 過去(P2-3 / P3-1 等)と同じ手順。確認後、一時ルート・検証ユーザー・スクリプト・
    スクリーンショットをすべて削除し、`git status` が空であることを確認済み
- 結果(いずれも実測値):
  - **M1 レイアウト**: `/calendar` 日表示・週表示、`/track`、`/summary` のすべてで
    **横スクロール 0px**(375px幅)。週表示は7列が収まり、曜日ラベルは 月・火・水・木・金・土・日
  - **最小localeの実地確認**: `/summary` のラベルが `2026年8月8日(土)` と正しく描画された
    (2026-08-08 は実際に土曜)。3-A の置換が実環境で機能していることを確認
  - **M2 backdrop-blur撤去後の視認性**: sticky バーは `backdropFilter: none` になり、
    背景95%不透明(ライト `oklab(.99…/0.95)` / ダーク `oklab(.217…/0.95)`)+境界線+影で
    背景との分離を維持。ライト/ダーク両方で確認
  - **M3 遅延ロードとフォーカストラップ**: 「予定を追加」で `role="dialog"`(aria-label「予定を追加」)が
    表示され、**フォーカスがダイアログ内(閉じるボタン)へ移動**。パネル幅375px・はみ出しなし
  - 併せて集計表示も正常(「+15分 (+17%)」「着手 予定より +15分遅れ」「-1時間 (-50%)」「未着手」)
  - タップターゲット44px未満の要素(再計測▶ 24px、日/週トグル 42×44、ワードマーク)は
    **いずれもP6-3で触っていない既存要素**。再計測▶はP5-4で意図的に小さくしている箇所
- 未解決・次にやること: P6-3 のPR作成。P6-5(本番の anon GRANT 剥奪)が未着手
