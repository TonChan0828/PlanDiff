# 2026-07-11

## 次の実装項目の検討 → P4-2 仕様書ドラフト作成

- やったこと: ユーザー指示「次の実装を検討してください」を受け、開発計画・指示資料
  (関連資料なし)を確認。「次に着手」上位3件はユーザー作業・保留のため、実装可能な
  Phase 4 から候補(P4-2 設定画面 / P4-1 オンボーディング / P4-3 LP)を提示し、
  ユーザーが P4-2 を選択。既存実装を調査(`/settings` はGoogle連携セクションのみ、
  ログアウトは `signOutAction` 既存、全テーブルが `auth.users` cascade 設計)し、
  `docs/specs/P4-2_設定画面.md` をドラフト作成。開発計画のP4-2を「承認待ち」に更新
- 理由: データ全削除はOAuth審査要件(Limited Use)であり凍結解除・P0-5再開の前提。
  スコープが明確で、/settings の骨格(P1-3)と cascade 設計(P0-6)を流用できるため
- 結果: 仕様書ドラフト完成(シナリオS1〜S12。単体9+結合3)。スキーマ変更なし
- 未解決・次にやること: 仕様書のユーザー承認待ち。承認後にシナリオ展開→TDD実装。
  mainに未コミットのdocs変更(2026-07-10ログ・開発計画)が残っており、P4-2のPRに同乗させる予定

## 00:20 P4-2 実装完了(設定画面: アカウント表示・ログアウト・データ全削除)

- やったこと: 仕様書承認(2026-07-11)を受けてTDDで実装。シナリオS1〜S12をテストに展開し
  Red確認(単体10件・結合2件が失敗)後にGreen化。
  (1) `lib/supabase/admin.ts` に `deleteUserAccount`(`auth.admin.deleteUser`。cascadeで
  全テーブル削除)。(2) `deleteAccountAction`(未認証→/login、失敗→
  `/settings?error=account_delete_failed`でセッション維持、成功→signOut→`/login?deleted=1`)。
  (3) `components/delete-account-section.tsx`(「削除」の完全一致入力までボタン無効)。
  (4) 設定ページにアカウントセクション(メール表示)・ログアウトボタン(既存
  `signOutAction` 再利用)・危険な操作セクションを追加。エラー表示をGoogle凍結と
  独立に出せるよう `GENERAL_ERROR_MESSAGES` に分離。(5) ログインページに
  `?deleted=1` の完了メッセージ。文言は `lib/settings/messages.ts` / `lib/auth/messages.ts` に集約
- 理由: データ全削除はOAuth審査要件(Limited Use)で凍結解除・P0-5再開の前提。
  凍結中の設定画面が実質空ページだった問題も解消
- 結果: 単体テスト196件(+10)・結合テスト58件(+2)全合格、`npm run check`
  (typecheck/lint/test/build)合格。シナリオ12件とテストの対応を突き合わせ確認
  (S4/S11は結合1件に統合、S4の単体側検証も追加)。Playwright 375pxで
  ログイン→設定表示(凍結中はGoogle導線なし・アカウント/ログアウト/削除表示)→
  「削除」入力でボタン有効化→削除実行→`/login?deleted=1` の完了メッセージ→
  DBの `auth.users`/`profiles` 0件を疎通確認。確認用ユーザーは削除フロー自体で消去
- 未解決・次にやること: コミット・PR(ユーザー指示待ち)。mainに残る2026-07-10の
  docs変更もこのPRに同乗させる

## 09:10 P4-2 コミット・PR作成

- やったこと: ユーザー指示「コミット」「プルリクエスト作成」。mainへ直接コミットしない
  ルールに従い `feat/p4-2-settings-account-delete` ブランチを作成し、P4-2の実装一式
  (16ファイル。設定画面のアカウント表示・ログアウト・データ全削除、仕様書・開発計画・
  ログの更新、2026-07-10分のdocs変更の同乗)をコミット。pre-commitフック
  (format/typecheck/lint/test 196件/build)全項目合格。push、PR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/13
- 未解決・次にやること: レビュー・マージ待ち。スキーマ変更なしのため本番への追加作業はなし

## 10:05 P4-2 マージ確認・main反映

- やったこと: ユーザーからPR #13マージの報告を受け、`gh pr view 13` でマージ済み
  (mergedAt 2026-07-11T01:04:06Z)を確認。ローカルを `main` に切替えて `git pull --ff-only`
  でfast-forward反映。開発計画の最終更新行を「PR #13マージ済み」に更新
- 結果: mainに反映済み。P4-2は完了。スキーマ変更なしのため本番Supabaseへの作業もなし
- 未解決・次にやること: この開発計画・ログの更新は次のフィーチャーブランチPRに同乗させる。
  次候補はP4-1(オンボーディング。Google凍結に合わせた内容の再設計が必要)または
  P4-3(LP)、およびユーザー作業のP1-3残(本番Supabase認証設定)・P3-3 M2(PWA実機確認)

## 10:30 P4-1 オンボーディング 仕様書ドラフト作成

- やったこと: ユーザーの「次の設計プランを立てて」指示を受け、次候補(P4-1/P4-3)を提示して
  P4-1(オンボーディング)の選択を確認。指示資料(READMEのみ)・要件定義書 §7・§13・
  開発計画・既存コード(`(app)/layout.tsx`、`calendar/page.tsx`、`settings/page.tsx`、
  `profiles` スキーマ)を確認し、`docs/specs/P4-1_オンボーディング.md` をドラフト作成。
  開発計画のP4-1を「承認待ち」に更新
- 理由: Google連携凍結中のため、要件定義書の「カレンダー連携の許可」ステップを除外し、
  アプリ内予定を前提とした3ステップ(予定→タイマー→ギャップ)に内容を再設計する必要があった。
  既読管理は複数デバイスで一貫させるため localStorage ではなく `profiles.onboarded_at`
  (timestamptz、要マイグレーション)を採用。既存RLSポリシーで本人限定の読み書きは担保済み
- 結果: 仕様書ドラフト作成済み(テストシナリオ S1〜S11 + 手動M1/M2)。実装は未着手
- 未解決・次にやること: ユーザーの仕様承認待ち。承認後にマイグレーション→テスト展開(Red)→実装

## 11:00 P4-1 オンボーディング 実装完了

- やったこと: ユーザー承認を受け仕様書ステータスを「承認済み」に更新。`feature/p4-1-onboarding`
  ブランチを作成し、db-migration Skillに従いマイグレーション
  `20260711011313_profiles_onboarded_at.sql`(`profiles.onboarded_at timestamptz`追加、
  新規RLSポリシー不要)を作成、`npx supabase db reset`→`db diff`で検証。TDDで
  `lib/onboarding/status.ts`(リダイレクト判定の純粋関数)・`lib/onboarding/complete.ts`
  (`markOnboardingComplete`。`.select("id")`で更新行数を見てRLS越境を検出する既存パターンに
  倣った)・`app/(app)/onboarding/actions.ts`・`app/(app)/onboarding/page.tsx`・
  `components/onboarding-steps.tsx`を実装。`calendar/page.tsx`にリダイレクト判定を追加、
  `settings/page.tsx`に「使い方をもう一度見る」リンクを追加(仕様書の「導線」節、S番号なし)。
  テストは先に失敗を確認(Red)してから実装(Green)。ローカルSupabaseでテストユーザーを作成し
  Playwrightで375px(ライト/ダーク)の全ステップ・スキップ・「はじめる」→`/calendar`遷移・
  再訪問時に再表示されないこと・設定画面からの再閲覧導線を実機確認。確認用ユーザーと
  スクリーンショットは後片付け済み
- 理由: calendar/page.tsx自体は既存の慣習(P2-1等)どおり専用テストを持たないため、
  S6/S7(リダイレクト判定)は抽出したlib関数`shouldRedirectToOnboarding`のテストで
  代替した。RLS越境検出は`timer/service.ts`の`updateTimeEntry`と同じ
  `.select().length===0`パターンを踏襲し、一貫性を優先した
- 結果: `npm run check`(typecheck/lint/test/build)全項目合格。単体テスト209件
  (P4-1で+13)、結合テスト61件(P4-1で+3)全合格。Playwright 375px確認(M1/M2)合格。
  開発計画のP4-1を「完了」に更新
- 未解決・次にやること: コミット・PR作成はユーザー指示待ち(mainへの直接コミット禁止のため
  featureブランチのまま)。本番Supabaseへのマイグレーション適用(`db push`)はPRマージ後に
  ユーザーが実行

## 11:10 P4-1 コミット・PR作成

- やったこと: ユーザー指示によりP4-1関連ファイルをコミット。pre-commitフックのformat:checkで
  `components/onboarding-steps.tsx`・`tests/integration/onboarding.test.ts`の2件が指摘され、
  `npm run format`で整形後に再コミット(format/typecheck/lint/test 209件/build全通過)。
  `feature/p4-1-onboarding`をpushしPR作成
- 結果: https://github.com/TonChan0828/PlanDiff/pull/14
- 未解決・次にやること: レビュー・マージ待ち。マージ後、本番Supabase(bkcppesfpayjtftlfmoz)へ
  `profiles.onboarded_at`カラム追加のマイグレーション適用(`db push`)がユーザー作業として残る

## 09:55 ローカル開発でのメール確認を不要化

- やったこと: ユーザー指示「localhostでもアカウント作成ができるようにして」を受けて調査。
  実際にPlaywrightでローカル(dev server + ローカルSupabase)のサインアップを試したところ、
  `/signup`から「確認メールを送信しました」まで正常に到達し、確認メール自体はMailpit
  (`http://127.0.0.1:54324`、ローカルSupabase同梱のテスト用メールキャッチャー)に届いていた。
  さらにログを確認すると、ユーザー本人が実メールアドレス(showxillia1028@yahoo.co.jp)で
  ほぼ同時刻にサインアップを試みた形跡があり、ローカルSupabaseは実際の受信箱にはメールを
  送らずMailpitに閉じ込める仕様のため、確認メールが届かず「アカウント作成ができない」と
  感じられたと判断。AskUserQuestionで目的を確認したところ「確認メール不要にする」を選択
  されたため、`supabase/config.toml` の `[auth.email] enable_confirmations` を
  ローカル限定でtrue→falseに変更(本番Supabaseの認証設定はダッシュボード管理でこのファイルの
  影響を受けないため、本番には影響しない)。`supabase stop`→`start`で設定を反映
  (データボリュームは保持されるため既存のテストユーザー等は消えない)
- 理由: 実メールアドレスでのサインアップ確認がローカル開発のボトルネックになっていたため。
  アプリのコード(signup-form.tsx等)は変更していない — enable_confirmations=falseだと
  Supabaseがsignup直後にセッションを返し、supabase-jsのクライアントが自動でセッションを
  保存するため、UI側の分岐を追加しなくても結果的に即ログイン状態になる
- 結果: Playwrightで新規メールアドレスによるサインアップ→確認メールなしで`/login`アクセス時に
  即座に`/onboarding`へリダイレクト(=既にログイン済み)されることを確認。
  `npm run check`(mainベース: 単体209件・build)全合格。UI文言(「確認メールを送信しました」)は
  ローカルでは実態と異なる表示のままだが、動作に支障はなくアプリコード変更を伴わないため許容。
  仕様書は作成していない(製品機能ではなくローカル開発環境限定の設定変更のため)
- 未解決・次にやること: コミット・PRはこのあと作成予定。本番Supabaseの確認必須設定は
  従来どおりダッシュボードで別管理(P1-3残タスクとして開発計画に既存記載あり)

## 09:57 ローカル確認メール不要化のPR作成

- やったこと: mainから分岐した`fix/local-skip-email-confirmation`ブランチをpushし、
  PR #16 を作成(https://github.com/TonChan0828/PlanDiff/pull/16)
- 理由: mainへの直接コミット禁止の運用に従う。P2-6のfeatureブランチとは無関係な変更のため
  別ブランチに分離した
- 結果: pre-commitフック全通過でコミット済み
- 未解決・次にやること: レビュー・マージ待ち。マージ後の本番作業は不要(ローカル限定の設定)
