# 2026-07-25

## 10:10 次の実装内容の調査・提案(P5-8着手)

- やったこと:
  - 開発計画・要件定義書・logs・specs・実装コードを調査。MVP(FR-01〜FR-13)は
    実装完了・宙に浮いた承認済み仕様はゼロで、残りは事業判断(ベータ投入・OAuth解除)と
    ユーザー作業(本番db push)が中心であることを確認
  - 核心価値「見積もりが当たるようになる」を深める新規候補を3つ提示
    (①ズレ%・開始遅延の可視化 / ②見積もり実測比の提示 / ③PWAオフライン対応)
  - ユーザーが①を選択 → P5-8 として着手
- 理由: 純粋な実装項目が消化済みで、次はユーザー選択による新規提案が必要な局面だったため
- 結果: 方向性①を確定。仕様書 P5-8 を作成し承認取得

## 10:12 P5-8 仕様書作成・承認

- やったこと: `docs/specs/P5-8_予定単位のズレと開始遅延.md` を作成。
  P5-3 で「将来検討」として見送った2項目(開始遅延・予定単位ズレ%)を実装対象にし、
  表示イメージ・算出定義・スコープ外・テストシナリオS1〜S11(正常/エッジ/境界/結合)を記載
- 理由: spec-driven-dev(仕様書→承認→TDD)の遵守
- 結果: ユーザー承認取得(文言は仕様案のまま。「着手 予定より +14分遅れ/3分早い/予定どおり」)。
  ステータスを承認済み(2026-07-25)へ更新

## 10:16 TDD実装(P5-8)

- やったこと:
  - Red: `tests/lib/summary/aggregate.test.ts`(P5-8 S1〜S9のうち単体7件)、
    `format.test.ts`(S10×3)、`app/(app)/summary/page.test.tsx`(S4・S7)、
    `tests/integration/summary.test.ts`(S11)を追加し失敗を確認。
    既存の `toEqual` テスト(aggregate S1・integration S12)も新フィールド追加に合わせ更新
  - Green: `lib/summary/aggregate.ts` の `GapSummaryItem` に `gapPercent`・`startDelayMinutes` を追加
    (開始遅延=予定に紐づく実績のうち最早開始 − 予定開始)。`lib/summary/format.ts` に
    `formatStartDelay` 追加(既存フォーマッタ流用)、`messages.ts` に文言集約、
    `summary/page.tsx` の内訳 `<li>` にズレ%併記(`gap-item-gap`)と開始遅延行(`gap-item-start-delay`)を追加
- 理由: 予定単位の振り返り解像度を上げ、核心価値を予定粒度まで下ろす。DB変更なし
- 結果:
  - `npm run check`(typecheck+lint+test+build)合格を出力で確認。単体/コンポーネント 466件全合格
    (既存454 → +12)
  - `npm run test:integration`(ローカルSupabase)88件全合格(S11含む)
  - 手動疎通(Playwright 375×667、一時 `/api/dev-login` + ローカル検証ユーザー1名):
    ライト/ダーク両方で「朝会 -5分(-17%)/着手 予定より 3分早い」「API設計 +22分(+37%)/
    着手 予定より +14分遅れ」「未着手予定は遅延・%非表示」を確認。横スクロールなし(scrollWidth=375)。
    完了後にルート・ユーザー・データ・スクショを削除し `git status` で残存なしを確認

## 未解決・次にやること

- PR作成(feature ブランチ)→ レビュー・マージ。マージ後に開発計画 P5-8 を「完了」へ更新
